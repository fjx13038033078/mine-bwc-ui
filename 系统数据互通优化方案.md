# 铜矿AI安全监控系统 - 数据互通优化方案

## 一、当前系统模块分析

### 1.1 现有模块清单（10个核心模块）

| 模块名称 | 路径 | 核心数据 | 当前状态 |
|---------|------|---------|---------|
| 综合看板 | dashboard | 汇总统计数据 | ✅ 已有数据流入 |
| 设备台账管理 | equipment/ledger | 设备信息 | ⚠️ 数据孤立 |
| 技能配置管理 | equipment/skill-config | 设备-技能关联 | ⚠️ 部分关联 |
| 视频管理 | equipment/video-management | 视频&AI结果 | ⚠️ 数据孤立 |
| 监控预警 | violation-monitor | 违规记录 | ⚠️ 新增模块，需打通 |
| 安全规范库 | library/safety-regulations | 规范条目 | ⚠️ 数据孤立 |
| 知识库 | library/violation-handling | 处理建议 | ⚠️ 数据孤立 |
| 文档库 | library/document-library | 制度文件 | ⚠️ 数据孤立 |
| AI技能管理 | skillsManagement/ai-skills | 技能状态 | ⚠️ 部分关联 |
| 提示词配置 | skillsManagement/prompt-config | 提示词 | ⚠️ 数据孤立 |

### 1.2 核心问题

❌ **问题1：数据孤岛**
- 各模块数据独立存储
- 修改一处不会影响其他模块
- 统计数据需要手动更新

❌ **问题2：业务断层**
- 视频识别结果不能自动推送到监控预警
- 违规处理不会更新设备统计
- 技能配置不影响视频识别

❌ **问题3：数据不一致**
- 同一设备在不同模块显示信息不同
- 统计数据可能出现偏差
- 缺乏统一的数据源

---

## 二、数据流转需求分析

### 2.1 铜矿实际业务流程

```
┌─────────────┐
│ 1. 技能管理 │ → 启用AI技能
└─────────────┘
       ↓
┌─────────────┐
│ 2. 设备部署 │ → 登记设备信息
└─────────────┘
       ↓
┌─────────────┐
│ 3. 技能配置 │ → 为设备绑定技能
└─────────────┘
       ↓
┌─────────────┐
│ 4. 视频采集 │ → AI自动识别
└─────────────┘
       ↓
┌─────────────┐
│ 5. 监控预警 │ → 违规自动推送
└─────────────┘
       ↓
┌─────────────┐
│ 6. 违规处理 │ → 查规范、查知识、查文档
└─────────────┘
       ↓
┌─────────────┐
│ 7. 数据归档 │ → 统计分析
└─────────────┘
```

### 2.2 关键数据流转关系

#### 流转1：设备 → 技能配置 → 视频管理
```
设备台账新增设备
  ↓
技能配置中可选该设备
  ↓
视频管理自动关联设备信息
  ↓
设备统计更新（视频数、违规数）
```

#### 流转2：视频识别 → 监控预警 → 违规处理
```
视频AI识别完成
  ↓
自动生成违规记录到监控预警
  ↓
监控预警处理违规
  ↓
更新设备违规统计
  ↓
更新部门违规统计
```

#### 流转3：违规类型 → 规范库 → 知识库 → 文档库
```
识别到违规类型
  ↓
匹配规范库中的规范要求
  ↓
获取知识库中的处理建议
  ↓
关联文档库中的处罚依据
  ↓
形成完整的处理方案
```

#### 流转4：技能管理 → 技能配置 → 提示词配置
```
AI技能启用/停用
  ↓
影响技能配置中的可选技能
  ↓
提示词配置只显示已启用技能
  ↓
视频识别使用对应提示词
```

---

## 三、数据互通解决方案

### 3.1 架构设计：统一数据中心

```
┌─────────────────────────────────────────────────────────┐
│                    Pinia Global Store                    │
│                    (统一数据中心)                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 设备数据  │  │ 技能数据  │  │ 视频数据  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 违规数据  │  │ 规范数据  │  │ 知识数据  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │ 文档数据  │  │ 提示词    │  │ 统计数据  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
│                                                          │
└─────────────────────────────────────────────────────────┘
                           ↓
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                   ↓
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │ 设备模块 │        │ 视频模块 │        │ 预警模块 │
   └─────────┘        └─────────┘        └─────────┘
        ↓                  ↓                   ↓
   实时读取            实时读取            实时读取
   自动更新            自动更新            自动更新
```

### 3.2 核心实现：Pinia Store 增强

#### Store结构设计

```typescript
// src/store/modules/copperMineData.ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';

export const useCopperMineStore = defineStore('copperMineData', () => {
  
  // ============ 状态定义 ============
  
  // 设备数据
  const devices = ref<DeviceVO[]>([]);
  
  // AI技能数据
  const aiSkills = ref<AISkillVO[]>([]);
  
  // 设备-技能关联数据
  const deviceSkillConfigs = ref<DeviceSkillConfigVO[]>([]);
  
  // 提示词配置数据
  const promptConfigs = ref<PromptConfigVO[]>([]);
  
  // 视频数据
  const videos = ref<VideoVO[]>([]);
  
  // 违规数据（新增）
  const violations = ref<ViolationVO[]>([]);
  
  // 规范数据
  const regulations = ref<RegulationVO[]>([]);
  
  // 知识库数据
  const knowledgeBase = ref<KnowledgeVO[]>([]);
  
  // 文档数据
  const documents = ref<DocumentVO[]>([]);
  
  
  // ============ 计算属性（自动统计） ============
  
  // 设备统计（实时计算）
  const deviceStats = computed(() => ({
    total: devices.value.length,
    online: devices.value.filter(d => d.status === 'online').length,
    offline: devices.value.filter(d => d.status === 'offline').length,
    maintenance: devices.value.filter(d => d.status === 'maintenance').length,
    faulty: devices.value.filter(d => d.status === 'faulty').length
  }));
  
  // 违规统计（实时计算）
  const violationStats = computed(() => ({
    total: violations.value.length,
    pending: violations.value.filter(v => v.status === 'pending').length,
    processing: violations.value.filter(v => v.status === 'processing').length,
    handled: violations.value.filter(v => v.status === 'handled').length,
    archived: violations.value.filter(v => v.status === 'archived').length,
    falseAlarm: violations.value.filter(v => v.status === 'false_alarm').length
  }));
  
  // 部门违规统计
  const departmentViolations = computed(() => {
    const stats: Record<string, number> = {};
    violations.value.forEach(v => {
      if (!stats[v.department]) stats[v.department] = 0;
      stats[v.department]++;
    });
    return stats;
  });
  
  // 违规类型统计
  const violationTypeStats = computed(() => {
    const stats: Record<string, number> = {};
    violations.value.forEach(v => {
      if (!stats[v.violationType]) stats[v.violationType] = 0;
      stats[v.violationType]++;
    });
    return Object.entries(stats)
      .sort((a, b) => b[1] - a[1])
      .slice(0, 10);
  });
  
  
  // ============ 数据操作方法（自动触发更新） ============
  
  // 设备操作
  const addDevice = (device: DeviceVO) => {
    devices.value.push(device);
    // 触发相关模块更新
    updateDeviceRelatedData(device.deviceId);
  };
  
  const updateDevice = (deviceId: string, updates: Partial<DeviceVO>) => {
    const index = devices.value.findIndex(d => d.deviceId === deviceId);
    if (index !== -1) {
      devices.value[index] = { ...devices.value[index], ...updates };
      updateDeviceRelatedData(deviceId);
    }
  };
  
  const deleteDevice = (deviceId: string) => {
    const index = devices.value.findIndex(d => d.deviceId === deviceId);
    if (index !== -1) {
      devices.value.splice(index, 1);
      // 清理关联数据
      cleanDeviceRelatedData(deviceId);
    }
  };
  
  // 视频操作（AI识别完成时自动生成违规记录）
  const updateVideoAIResult = (videoId: string, aiResult: AIResult) => {
    const video = videos.value.find(v => v.videoId === videoId);
    if (!video) return;
    
    video.aiStatus = 'completed';
    video.aiResult = aiResult;
    
    // 如果AI检测到违规，自动生成违规记录到监控预警
    if (aiResult.violations && aiResult.violations.length > 0) {
      aiResult.violations.forEach(violation => {
        const device = devices.value.find(d => d.deviceId === video.deviceId);
        
        const newViolation: ViolationVO = {
          violationId: generateId('VIO'),
          videoId: video.videoId,
          deviceId: video.deviceId,
          captureTime: video.captureTime,
          violationType: violation.type,
          severity: violation.severity,
          description: violation.description,
          department: device?.department || '未知部门',
          principal: device?.principal || '未知',
          status: 'pending',
          reported: false,
          snapshotUrl: video.thumbnailUrl,
          videoUrl: video.videoUrl,
          violationTimestamp: violation.timestamp,
          confidence: violation.confidence,
          matchedRegulation: matchRegulation(violation.type),
          handlingSuggestion: matchKnowledge(violation.type)
        };
        
        violations.value.push(newViolation);
        
        // 更新设备违规统计
        if (device) {
          device.violationCount = (device.violationCount || 0) + 1;
        }
      });
    }
  };
  
  // 违规处理（更新相关统计）
  const updateViolationStatus = (
    violationId: string, 
    status: string, 
    handler?: string,
    handleTime?: string
  ) => {
    const violation = violations.value.find(v => v.violationId === violationId);
    if (!violation) return;
    
    violation.status = status as any;
    violation.handler = handler;
    violation.handleTime = handleTime;
    
    // 如果标记为误报，减少设备违规统计
    if (status === 'false_alarm') {
      const device = devices.value.find(d => d.deviceId === violation.deviceId);
      if (device && device.violationCount > 0) {
        device.violationCount--;
      }
    }
  };
  
  // 技能配置（影响视频识别能力）
  const configureDeviceSkills = (deviceId: string, skillIds: string[]) => {
    // 删除旧配置
    deviceSkillConfigs.value = deviceSkillConfigs.value.filter(
      c => c.deviceId !== deviceId
    );
    
    // 添加新配置
    skillIds.forEach(skillId => {
      deviceSkillConfigs.value.push({
        deviceId,
        skillId,
        configTime: new Date().toISOString()
      });
    });
    
    // 更新技能使用统计
    updateSkillUsageStats();
  };
  
  // 规范匹配（自动匹配）
  const matchRegulation = (violationType: string) => {
    const regulation = regulations.value.find(r => 
      r.violations.includes(violationType)
    );
    
    if (!regulation) return undefined;
    
    return {
      workType: regulation.workType,
      violation: violationType,
      standard: regulation.standards.join('；')
    };
  };
  
  // 知识匹配（自动匹配）
  const matchKnowledge = (violationType: string) => {
    const knowledge = knowledgeBase.value.find(k => 
      k.violationType === violationType
    );
    
    if (!knowledge) return undefined;
    
    return {
      measure: knowledge.measure,
      basis: knowledge.basis,
      prevention: knowledge.prevention
    };
  };
  
  // 辅助方法
  const updateDeviceRelatedData = (deviceId: string) => {
    // 更新相关模块的数据
    // 例如：更新视频列表中的设备信息、更新违规记录中的设备信息等
  };
  
  const cleanDeviceRelatedData = (deviceId: string) => {
    // 清理设备删除后的关联数据
    deviceSkillConfigs.value = deviceSkillConfigs.value.filter(
      c => c.deviceId !== deviceId
    );
  };
  
  const updateSkillUsageStats = () => {
    // 更新技能使用统计
    aiSkills.value.forEach(skill => {
      skill.deviceCount = deviceSkillConfigs.value.filter(
        c => c.skillId === skill.id
      ).length;
    });
  };
  
  const generateId = (prefix: string) => {
    const timestamp = Date.now();
    const random = Math.floor(Math.random() * 1000);
    return `${prefix}${timestamp}${random}`;
  };
  
  
  // ============ 返回 ============
  return {
    // 状态
    devices,
    aiSkills,
    deviceSkillConfigs,
    promptConfigs,
    videos,
    violations,
    regulations,
    knowledgeBase,
    documents,
    
    // 计算属性
    deviceStats,
    violationStats,
    departmentViolations,
    violationTypeStats,
    
    // 方法
    addDevice,
    updateDevice,
    deleteDevice,
    updateVideoAIResult,
    updateViolationStatus,
    configureDeviceSkills,
    matchRegulation,
    matchKnowledge
  };
});
```

---

## 四、具体模块数据互通实现

### 4.1 设备台账 → 技能配置

**数据流**：
```typescript
// 设备台账新增设备
const handleAddDevice = () => {
  const store = useCopperMineStore();
  store.addDevice(newDevice);
  // 技能配置模块自动可以选择这个新设备
};
```

**技能配置模块**：
```typescript
// 技能配置中获取设备列表
const store = useCopperMineStore();
const deviceList = computed(() => store.devices);
```

### 4.2 视频管理 → 监控预警

**数据流**：
```typescript
// 视频AI识别完成
const handleAIComplete = (videoId: string, aiResult: AIResult) => {
  const store = useCopperMineStore();
  // 更新视频结果，自动生成违规记录
  store.updateVideoAIResult(videoId, aiResult);
  
  // 监控预警模块会自动显示新的违规记录
};
```

**监控预警模块**：
```typescript
// 监控预警实时获取违规数据
const store = useCopperMineStore();
const violationList = computed(() => store.violations);
```

### 4.3 监控预警 → 设备统计

**数据流**：
```typescript
// 处理违规
const handleProcessViolation = (violationId: string) => {
  const store = useCopperMineStore();
  store.updateViolationStatus(violationId, 'handled', '张三', new Date());
  
  // 设备台账中的设备违规统计自动更新
  // 综合看板中的违规统计自动更新
};
```

### 4.4 违规类型 → 规范库 + 知识库

**自动匹配**：
```typescript
// AI识别到违规时，自动匹配规范和知识
const aiResult = {
  violations: [{
    type: '未佩戴安全帽',
    // ... 其他字段
  }]
};

// Store会自动调用
const regulation = store.matchRegulation('未佩戴安全帽');
const knowledge = store.matchKnowledge('未佩戴安全帽');

// 监控预警详情中自动显示
```

### 4.5 技能管理 → 提示词配置

**数据联动**：
```typescript
// 技能启用/停用
const handleToggleSkill = (skillId: string, status: 'enabled' | 'disabled') => {
  const store = useCopperMineStore();
  const skill = store.aiSkills.find(s => s.id === skillId);
  if (skill) {
    skill.status = status;
  }
  
  // 提示词配置模块只显示已启用的技能
};

// 提示词配置模块
const enabledSkills = computed(() => 
  store.aiSkills.filter(s => s.status === 'enabled')
);
```

---

## 五、数据一致性保证

### 5.1 单一数据源原则

✅ **所有数据存储在Pinia Store**
- 各模块只读取Store数据
- 所有修改通过Store方法进行
- 避免模块内部维护独立数据

### 5.2 响应式更新

✅ **使用Vue 3 Reactive**
```typescript
// 在组件中使用computed自动响应
const violationList = computed(() => {
  const store = useCopperMineStore();
  return store.violations.filter(v => v.status === 'pending');
});

// Store数据变化，组件自动更新
```

### 5.3 事务性操作

✅ **关联数据同时更新**
```typescript
const deleteDevice = (deviceId: string) => {
  // 1. 删除设备
  devices.value = devices.value.filter(d => d.deviceId !== deviceId);
  
  // 2. 删除技能配置
  deviceSkillConfigs.value = deviceSkillConfigs.value.filter(
    c => c.deviceId !== deviceId
  );
  
  // 3. 更新视频记录状态
  videos.value.forEach(v => {
    if (v.deviceId === deviceId) {
      v.deviceStatus = 'deleted';
    }
  });
  
  // 4. 更新违规记录备注
  violations.value.forEach(v => {
    if (v.deviceId === deviceId) {
      v.remark = '设备已删除';
    }
  });
};
```

---

## 六、实施步骤

### Phase 1: Store升级（Week 1）
- [ ] 升级copperMineData.ts Store
- [ ] 添加完整的数据模型
- [ ] 实现所有CRUD方法
- [ ] 实现自动匹配逻辑

### Phase 2: 核心模块改造（Week 2）
- [ ] 改造设备台账，使用Store
- [ ] 改造视频管理，接入Store
- [ ] 改造监控预警，接入Store
- [ ] 测试数据流转

### Phase 3: 其他模块接入（Week 3）
- [ ] 技能配置接入Store
- [ ] 规范库接入Store
- [ ] 知识库接入Store
- [ ] 文档库接入Store

### Phase 4: 数据初始化和测试（Week 4）
- [ ] 准备完整的模拟数据
- [ ] 测试各模块数据互通
- [ ] 测试统计自动更新
- [ ] 性能优化

---

## 七、预期效果

### 7.1 业务流程自动化

✅ **场景1：设备新增流程**
```
设备台账新增设备
  ↓ (自动)
技能配置可选该设备
  ↓ (自动)
视频管理显示该设备
  ↓ (自动)
综合看板更新设备统计
```

✅ **场景2：违规处理流程**
```
视频AI识别完成
  ↓ (自动)
监控预警新增违规记录
  ↓ (自动)
匹配规范库和知识库
  ↓ (手动)
安全员处理违规
  ↓ (自动)
更新设备违规统计
  ↓ (自动)
更新部门违规统计
  ↓ (自动)
综合看板更新图表
```

### 7.2 数据实时同步

✅ **实时统计**
- 设备数量、状态实时更新
- 违规数量、状态实时更新
- 部门统计实时更新
- 图表自动刷新

✅ **数据一致性**
- 同一设备在所有模块信息一致
- 违规统计在所有地方数据一致
- 无需手动刷新或重新加载

### 7.3 用户体验提升

✅ **操作简化**
- 一次操作，多处生效
- 无需重复录入数据
- 自动关联相关信息

✅ **信息透明**
- 实时看到数据变化
- 清晰的数据流转轨迹
- 完整的业务闭环

---

## 八、技术方案总结

### 8.1 核心技术栈

- **Pinia**: 全局状态管理
- **Vue 3 Reactive**: 响应式数据
- **Computed**: 自动计算统计
- **TypeScript**: 类型安全

### 8.2 架构优势

✅ **单一数据源**: 避免数据不一致
✅ **响应式更新**: 自动同步所有组件
✅ **类型安全**: TypeScript保证数据结构
✅ **易于维护**: 集中管理，便于调试

### 8.3 性能优化

- 使用computed缓存计算结果
- 只更新变化的数据
- 按需加载大数据
- 虚拟滚动优化列表

---

## 九、后续扩展

### 9.1 数据持久化

- LocalStorage缓存
- IndexedDB大数据存储
- 接入后端API

### 9.2 实时通信

- WebSocket实时推送
- 服务端主动通知
- 多用户协同

### 9.3 数据分析

- 时间序列分析
- 趋势预测
- 智能告警

---

**数据互通，业务贯通，管理畅通！**

