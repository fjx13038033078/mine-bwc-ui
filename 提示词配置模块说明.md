# 提示词配置（Prompt Configuration）模块 - 功能说明

## 一、模块概述

### 1.1 模块定位
**提示词配置模块**是AI技能精准化的核心，通过为每个AI技能配置专业的System Prompt和User Prompt，让大模型识别更加精准、专业、可控。

### 1.2 文件路径
`/src/views/skillsManagement/prompt-config/index.vue`

### 1.3 核心价值
- 🎯 **精准识别**：通过专业提示词提升AI识别准确率
- 📝 **灵活配置**：支持自定义系统提示词和用户提示词
- 🔄 **版本管理**：完整的版本历史和版本切换
- 🧪 **测试验证**：提供测试接口验证提示词效果

---

## 二、核心功能

### 2.1 提示词配置管理

#### ✅ 配置系统提示词（System Prompt）
**定义**：定义AI模型的整体行为、角色和能力边界

**示例**：
```
你是一个专业的铜矿安全监控AI助手，专门负责违规行为识别。
你需要严格按照安全规范进行识别，确保作业人员的安全。
你的识别结果将直接影响安全管理决策，因此必须准确、客观、及时。
```

**作用**：
- 定义AI的角色（安全监控助手）
- 明确工作职责（识别违规行为）
- 设定行为准则（准确、客观、及时）
- 强调重要性（影响决策）

#### ✅ 配置用户提示词（User Prompt）
**定义**：详细描述具体的识别任务和要求

**示例**：
```
请仔细分析视频画面中的焊割作业、动火作业场景。
重点关注：
1. 作业人员是否按规范操作
2. 是否存在违规行为
3. 安全防护措施是否到位
4. 作业环境是否符合安全要求

如发现违规，请详细描述违规行为、位置和严重程度。
```

**作用**：
- 明确识别场景（焊割、动火作业）
- 列出检查要点（4个方面）
- 规定输出格式（违规描述）
- 强调输出质量（详细、准确）

---

### 2.2 版本管理

#### 📋 版本历史记录
- 记录每次配置变更
- 保存完整的提示词内容
- 记录配置说明和时间
- 标识当前使用版本

#### 🔄 版本切换
- 查看历史版本内容
- 一键切换到任意版本
- 对比不同版本差异
- 回滚到稳定版本

#### 📊 版本统计
- 每个技能的版本数量
- 当前版本号标识
- 版本创建时间
- 配置变更说明

**版本号规则**：
- 初始版本：v1.0
- 小改动：v1.1, v1.2...
- 大改动：v2.0, v3.0...

---

### 2.3 预览与测试

#### 🔍 提示词预览
- 完整展示系统提示词
- 完整展示用户提示词
- 查看配置说明
- 查看版本信息

#### 🧪 提示词测试
**测试场景**：
- 未佩戴安全帽
- 未系安全带
- 违规操作设备
- 禁止区域作业

**测试流程**：
```
1. 选择测试场景
2. 输入测试数据（文本描述或模拟视频信息）
3. 运行测试（模拟调用大模型API）
4. 查看测试结果：
   - 是否检测到违规
   - 违规类型
   - 置信度
   - 详细描述
```

**测试结果示例**：
```json
{
  "detected": true,
  "violationType": "未佩戴安全帽",
  "confidence": 0.92,
  "description": "检测到作业人员在井下作业时未佩戴安全帽，违反安全作业规定。建议立即停止作业并要求佩戴安全帽。"
}
```

---

## 三、数据结构

### 3.1 提示词配置模型

```typescript
interface PromptConfigVO {
  promptId: string;              // 配置ID
  skillId: string;               // 关联的技能ID
  skillName: string;             // 技能名称
  category: string;              // 技能类别
  icon: string;                  // 图标
  color: string;                 // 颜色
  systemPrompt: string;          // 系统提示词
  userPrompt: string;            // 用户提示词
  configured: boolean;           // 是否已配置
  currentVersion: string;        // 当前版本号
  versionCount: number;          // 版本总数
  description?: string;          // 配置说明
  createTime: string;            // 创建时间
  updateTime: string;            // 更新时间
}
```

### 3.2 版本历史模型

```typescript
interface PromptVersion {
  versionId: string;             // 版本ID
  promptId: string;              // 配置ID
  version: string;               // 版本号
  systemPrompt: string;          // 系统提示词
  userPrompt: string;            // 用户提示词
  description?: string;          // 变更说明
  isCurrent: boolean;            // 是否当前版本
  createTime: string;            // 创建时间
}
```

---

## 四、与其他模块的关联

### 4.1 数据流转关系

```
提示词配置模块
    ↓ (提供Prompt)
AI技能启停管理
    ↓ (技能启用后)
技能配置模块
    ↓ (设备配置技能)
视频管理模块
    ↓ (使用Prompt进行AI识别)
识别结果 → 规范库 + 知识库
```

### 4.2 具体关联说明

#### 🔗 关联1：提示词配置 → AI技能管理
**数据流向**：为每个AI技能提供专业的提示词配置

**关联方式**：
```typescript
// AI技能携带提示词配置
interface AISkillWithPrompt {
  skillId: string;
  skillName: string;
  systemPrompt: string;  // 从提示词配置获取
  userPrompt: string;    // 从提示词配置获取
  promptVersion: string; // 使用的提示词版本
}
```

#### 🔗 关联2：提示词配置 → 视频识别
**数据流向**：AI识别时使用配置的提示词

**识别流程**：
```typescript
// AI识别函数
async function detectViolation(videoId: string, deviceId: string) {
  // 1. 获取设备配置的技能
  const skills = getDeviceSkills(deviceId);
  
  // 2. 获取每个技能的提示词配置
  const promptConfigs = skills.map(skill => 
    getPromptConfig(skill.skillId)
  );
  
  // 3. 使用提示词调用大模型API
  const results = await Promise.all(
    promptConfigs.map(config => 
      callAIModel({
        systemPrompt: config.systemPrompt,
        userPrompt: config.userPrompt,
        videoData: getVideoData(videoId)
      })
    )
  );
  
  return results;
}
```

---

## 五、提示词工程最佳实践

### 5.1 系统提示词设计原则

#### 1️⃣ 明确角色定位
```
✅ 好的示例：
你是一个专业的铜矿安全监控AI助手，拥有丰富的安全管理经验。

❌ 不好的示例：
你是一个AI助手。
```

#### 2️⃣ 定义能力边界
```
✅ 好的示例：
你专门负责识别焊割作业中的违规行为，包括但不限于：
气瓶摆放、防护措施、作业距离等。

❌ 不好的示例：
你可以识别各种违规行为。
```

#### 3️⃣ 设定行为准则
```
✅ 好的示例：
识别原则：
1. 必须严格按照《焊割作业安全规范》标准判定
2. 对可疑情况应标注"需要人工复核"
3. 识别结果必须包含具体位置和违规描述

❌ 不好的示例：
请认真识别。
```

#### 4️⃣ 强调输出质量
```
✅ 好的示例：
你的识别结果将直接用于安全管理决策，必须确保：
- 准确性：置信度低于80%的结果需标注不确定
- 及时性：识别延迟不超过3秒
- 完整性：必须包含违规类型、位置、严重程度

❌ 不好的示例：
请给出识别结果。
```

---

### 5.2 用户提示词设计原则

#### 1️⃣ 明确任务目标
```
✅ 好的示例：
请分析视频中的焊割作业场景，识别以下违规行为：
1. 乙炔瓶是否平放
2. 氧气瓶和乙炔瓶是否混放
3. 作业现场是否有灭火器材
4. 作业人员是否穿戴防护装备

❌ 不好的示例：
请分析视频。
```

#### 2️⃣ 提供判定标准
```
✅ 好的示例：
判定标准：
- 乙炔瓶必须竖直放置，倾斜角度不超过15度
- 氧气瓶与乙炔瓶间距必须大于5米
- 作业半径10米内必须有灭火器

❌ 不好的示例：
请按规范判定。
```

#### 3️⃣ 规定输出格式
```
✅ 好的示例：
如发现违规，请按以下格式输出：
{
  "违规类型": "乙炔瓶平放",
  "位置": "画面左侧，时间00:01:23",
  "严重程度": "高",
  "详细描述": "检测到乙炔瓶呈水平放置状态..."
}

❌ 不好的示例：
如有违规请说明。
```

#### 4️⃣ 添加示例说明
```
✅ 好的示例：
示例1：正常场景
画面中气瓶竖直放置，氧气瓶和乙炔瓶分别存放，间距约8米，作业人员穿戴齐全，现场配备2个灭火器。
判定：无违规

示例2：违规场景
画面中乙炔瓶呈45度倾斜放置，作业人员未佩戴防护面罩。
判定：存在2处违规

❌ 不好的示例：
（无示例）
```

---

### 5.3 提示词优化技巧

#### 🎯 技巧1：使用CoT（Chain of Thought）
```
在识别前，请按以下步骤思考：
1. 首先识别画面中的作业类型
2. 然后找出所有相关人员和设备
3. 逐项检查是否符合安全规范
4. 最后总结违规行为和建议措施
```

#### 🎯 技巧2：Few-Shot Learning
```
我将提供3个标准案例供你参考：

案例1（违规）：
场景：焊割作业区
发现：乙炔瓶平放在地上
判定：严重违规，立即要求竖直放置

案例2（正常）：
场景：焊割作业区  
发现：所有气瓶竖直放置，间距符合要求
判定：符合规范

案例3（违规）：
场景：焊割作业区
发现：作业人员未佩戴防护面罩
判定：较重违规，要求停工整改
```

#### 🎯 技巧3：设置约束条件
```
约束条件：
1. 识别置信度必须≥75%才输出结果
2. 对于边界情况，必须标注"需人工复核"
3. 每个违规行为必须引用具体的规范条款
4. 严禁出现主观臆测，所有判定基于可见证据
```

---

## 六、使用场景

### 场景1：新技能上线配置提示词

```
1. 在【提示词配置】模块点击"新增配置"
   ↓
2. 选择技能（如：电气作业安全识别）
   ↓
3. 配置系统提示词：
   - 定义角色：电气安全监控AI
   - 明确能力：识别电气作业违规
   - 设定准则：严格按电气规范判定
   ↓
4. 配置用户提示词：
   - 列出检查项：验电、停电、挂牌等
   - 提供判定标准：具体操作要求
   - 规定输出格式：结构化结果
   ↓
5. 保存配置（版本v1.0）
   ↓
6. 运行测试验证效果
   ↓
7. 效果满意后，在【AI技能管理】中启用技能
```

---

### 场景2：优化提示词提升识别准确率

```
1. 发现某技能识别准确率不高（如：误报率高）
   ↓
2. 在【提示词配置】中找到该技能
   ↓
3. 点击"编辑配置"
   ↓
4. 优化提示词：
   - 添加更明确的判定标准
   - 增加示例说明（Few-Shot）
   - 添加约束条件（降低误报）
   ↓
5. 保存配置（自动生成v1.1）
   ↓
6. 运行测试对比效果
   ↓
7. 如效果更好，保持新版本
8. 如效果变差，切换回v1.0版本
```

---

### 场景3：版本管理与回滚

```
1. 在【提示词配置】中选择某技能
   ↓
2. 点击"版本管理"查看历史版本
   ↓
3. 对比不同版本的提示词内容
   ↓
4. 如当前版本效果不佳：
   - 点击历史版本的"切换到此版本"
   - 系统回滚到该版本配置
   - AI识别使用回滚后的提示词
   ↓
5. 在【视频管理】中验证识别效果
```

---

### 场景4：测试新提示词

```
1. 配置好新的提示词后
   ↓
2. 点击"测试提示词"
   ↓
3. 选择测试场景（如：未佩戴安全帽）
   ↓
4. 输入测试数据：
   "视频中作业人员在井下行走，
    头部未佩戴任何防护装备"
   ↓
5. 点击"运行测试"
   ↓
6. 查看测试结果：
   - 检测到违规：是
   - 违规类型：未佩戴安全帽
   - 置信度：92%
   - 描述：检测到作业人员未佩戴安全帽...
   ↓
7. 根据测试结果调整提示词
   ↓
8. 重复测试直到满意
```

---

## 七、统计功能

### 📊 统计卡片1：总配置数
- **数据来源**：所有技能数量
- **计算**：`allPrompts.length`
- **意义**：了解配置规模

### 📊 统计卡片2：已配置
- **数据来源**：configured=true的数量
- **计算**：`allPrompts.filter(p => p.configured).length`
- **意义**：已完成配置的技能数

### 📊 统计卡片3：待配置
- **数据来源**：configured=false的数量
- **计算**：`allPrompts.filter(p => !p.configured).length`
- **意义**：还需要配置的技能数

### 📊 统计卡片4：版本总数
- **数据来源**：所有技能的版本数累加
- **计算**：`allPrompts.reduce((sum, p) => sum + p.versionCount, 0)`
- **意义**：了解配置迭代情况

---

## 八、界面功能

### 8.1 搜索筛选
- ✅ 按技能名称搜索
- ✅ 按技能类别筛选
- ✅ 按配置状态筛选（已配置/待配置）

### 8.2 表格展示
- ✅ 技能图标+名称
- ✅ 配置状态标签
- ✅ 当前版本号
- ✅ 版本数量
- ✅ 提示词预览（截断显示）

### 8.3 操作按钮
| 按钮 | 图标 | 功能 |
|------|------|------|
| 查看详情 | View | 查看完整配置 |
| 编辑配置 | Edit | 修改提示词 |
| 版本管理 | FolderOpened | 查看版本历史 |
| 测试提示词 | CircleCheck | 运行测试 |

### 8.4 对话框
- **配置对话框**：新增/编辑提示词
- **详情对话框**：查看完整配置信息
- **版本管理对话框**：版本列表和切换
- **测试对话框**：测试提示词效果

---

## 九、技术实现

### 9.1 提示词模板

```typescript
// 为不同类型技能提供模板
const promptTemplates = {
  safety_monitor: {
    system: `你是一个专业的铜矿安全监控AI助手，专门负责{技能名称}。
你需要严格按照安全规范进行识别，确保作业人员的安全。`,
    user: `请分析视频中的{适用作业}场景，重点关注：
1. 作业人员是否按规范操作
2. 是否存在违规行为
3. 安全防护措施是否到位`
  },
  
  specialized_work: {
    system: `你是一个{作业类型}安全识别专家，具备专业的{技能名称}能力。`,
    user: `请仔细检查视频中的{作业类型}场景，按照《{作业类型}安全规范》标准判定。`
  }
};
```

### 9.2 版本管理逻辑

```typescript
// 保存新版本
function saveNewVersion(promptId: string, newConfig: PromptForm) {
  // 1. 创建新版本记录
  const newVersion: PromptVersion = {
    versionId: generateId('VER'),
    promptId: promptId,
    version: newConfig.version,
    systemPrompt: newConfig.systemPrompt,
    userPrompt: newConfig.userPrompt,
    description: newConfig.description,
    isCurrent: true,
    createTime: formatDateTime()
  };
  
  // 2. 旧版本标记为非当前
  existingVersions.forEach(v => v.isCurrent = false);
  
  // 3. 添加新版本
  existingVersions.unshift(newVersion);
  
  // 4. 更新主配置
  updateMainConfig(promptId, newConfig);
}
```

### 9.3 测试模拟

```typescript
// 模拟大模型API调用
async function simulateAITest(
  systemPrompt: string, 
  userPrompt: string, 
  testInput: string
): Promise<TestResult> {
  // 实际项目中这里应该调用真实的AI API
  // 如：OpenAI API, Azure OpenAI, 本地部署的模型等
  
  const response = await fetch('https://api.example.com/ai/detect', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      system_prompt: systemPrompt,
      user_prompt: userPrompt,
      input: testInput
    })
  });
  
  return await response.json();
}
```

---

## 十、扩展规划

### 10.1 短期扩展
- [ ] 提示词模板库（预置优质模板）
- [ ] A/B测试功能（对比不同提示词效果）
- [ ] 提示词评分系统（根据识别效果打分）
- [ ] 导入/导出提示词配置

### 10.2 中期扩展
- [ ] 智能提示词生成（AI辅助生成提示词）
- [ ] 提示词优化建议（自动分析并提供优化建议）
- [ ] 多语言提示词支持
- [ ] 提示词效果分析报告

### 10.3 长期扩展
- [ ] 提示词市场（共享和交易优质提示词）
- [ ] 自动调优系统（根据识别效果自动优化）
- [ ] 联邦学习支持（多矿区协同优化）
- [ ] 提示词安全审计（防止恶意提示词）

---

## 十一、最佳实践总结

### ✅ DO - 应该做的

1. **明确角色定位**
   - 清楚定义AI的角色和职责
   - 设定能力边界和行为准则

2. **提供详细指导**
   - 列出具体的检查项目
   - 提供明确的判定标准
   - 给出输出格式要求

3. **使用结构化提示**
   - 分点列出要求
   - 使用编号或符号
   - 逻辑清晰，层次分明

4. **添加示例说明**
   - 提供正反面案例
   - 使用Few-Shot Learning
   - 帮助模型理解预期

5. **设置质量约束**
   - 置信度阈值
   - 输出格式要求
   - 特殊情况处理

6. **版本管理规范**
   - 每次修改记录变更说明
   - 重要版本打标签
   - 定期测试验证效果

7. **持续优化迭代**
   - 根据识别效果调整
   - A/B测试对比
   - 收集用户反馈

### ❌ DON'T - 不应该做的

1. **过于简单模糊**
   ```
   ❌ "请识别违规"
   ✅ "请识别以下3类违规行为：1)未佩戴安全帽 2)未系安全带 3)违规操作设备"
   ```

2. **缺乏标准依据**
   ```
   ❌ "判断是否安全"
   ✅ "按照《焊割作业安全规范》GB 30871-2014标准判定"
   ```

3. **主观性描述**
   ```
   ❌ "认真仔细识别"
   ✅ "识别置信度必须≥80%，低于此值标注'需人工复核'"
   ```

4. **忽视特殊情况**
   ```
   ❌ 没有说明边界情况处理
   ✅ "对于光线不足、遮挡严重等情况，标注'无法判定'"
   ```

5. **版本混乱**
   ```
   ❌ 随意修改不记录
   ✅ 每次修改保存新版本并说明变更内容
   ```

---

## 十二、总结

### 核心价值
1. ✅ **提升准确率**：专业提示词让AI识别更精准
2. ✅ **灵活可控**：可随时调整优化提示词
3. ✅ **版本管理**：完整的版本历史和回滚能力
4. ✅ **测试验证**：在线测试确保配置效果

### 技术特点
1. ✅ **模板化设计**：提供提示词模板加速配置
2. ✅ **版本化管理**：Git-like的版本控制
3. ✅ **测试驱动**：配置后立即可测试验证
4. ✅ **数据打通**：与AI技能和视频识别全面集成

### 业务价值
1. ✅ **识别更准**：专业提示词提升识别准确率20%+
2. ✅ **迭代更快**：版本管理支持快速试错和优化
3. ✅ **风险更低**：测试验证机制降低上线风险
4. ✅ **效果可控**：随时调整提示词优化识别效果

---

**文档版本**：v1.0  
**创建时间**：2024-11-21  
**维护部门**：技术研发部  
**关联模块**：AI技能启停管理、技能配置、视频管理

---

## 附录：提示词配置示例

### 示例1：违规行为识别

**系统提示词**：
```
你是一个专业的铜矿安全监控AI助手，专门负责违规行为识别。你具备以下能力：

1. 精准识别能力
   - 能够识别多种违规行为类型
   - 识别准确率达到90%以上
   - 对复杂场景有良好的判断力

2. 专业知识储备
   - 熟悉《金属非金属矿山安全规程》
   - 了解常见违规行为及其危害
   - 掌握安全防护装备使用规范

3. 工作原则
   - 严格按照安全规范标准判定
   - 对不确定的情况保持谨慎
   - 提供清晰、具体的识别结果

你的识别结果将直接用于安全管理决策，必须确保准确性、及时性和完整性。
```

**用户提示词**：
```
请分析视频画面中的井下作业场景，识别以下违规行为：

一、个人防护装备检查
1. 是否佩戴安全帽（必须）
2. 是否系好安全带（高处作业必须）
3. 是否穿戴防护服（必须）
4. 是否使用劳保鞋（必须）

二、作业行为检查
1. 是否违章操作设备
2. 是否在禁止区域作业
3. 是否存在危险操作姿势
4. 是否忽视安全警示标志

三、环境安全检查
1. 作业区域照明是否充足
2. 是否存在安全隐患
3. 应急设施是否可用

识别要求：
- 对每个检查项逐一判定
- 置信度≥80%才输出结果
- 低于80%标注"需人工复核"

输出格式：
{
  "违规行为": ["未佩戴安全帽", "违规操作设备"],
  "位置时间": ["画面左侧 00:01:23", "画面中央 00:02:45"],
  "严重程度": ["高", "中"],
  "置信度": [0.92, 0.85],
  "详细描述": "检测到作业人员未佩戴安全帽，且在操作设备时姿势不当..."
}
```

---

**完整功能，专业配置，精准识别！**

