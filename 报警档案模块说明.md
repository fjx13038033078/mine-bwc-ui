# 报警档案模块说明文档

## 一、模块概述

### 1.1 模块定位
报警档案模块是铜矿AI安全监控系统的统计分析模块，用于对监控预警模块中已归档的违规数据进行周期性统计、分析和归档管理。

### 1.2 核心功能
- 📊 **统计分析**：按日/周/月统计设备检查和违规情况
- 📁 **档案管理**：新增、编辑、删除、查看档案记录
- 📈 **数据展示**：多维度统计指标可视化展示
- 📤 **报表导出**：支持CSV格式导出统计报表
- 🔍 **查询筛选**：按周期、时间、部门筛选档案

### 1.3 业务价值
- ✅ 实现违规数据的系统化归档
- ✅ 提供周期性的统计分析报告
- ✅ 支撑管理层的决策分析
- ✅ 满足安全管理的合规要求

---

## 二、数据字段说明

### 2.1 核心统计字段

| 字段名称 | 类型 | 说明 | 业务含义 |
|---------|------|------|---------|
| archiveId | string | 档案编号 | 系统自动生成，格式：ARC+时间戳 |
| period | string | 统计周期 | daily(日统计)/weekly(周统计)/monthly(月统计) |
| statisticDate | string | 统计日期 | 统计的基准日期 |
| department | string | 部门 | 所属部门，可为空表示全部 |

### 2.2 人员覆盖指标

| 字段名称 | 类型 | 说明 | 计算方式 |
|---------|------|------|---------|
| coveragePeople | number | 覆盖人数 | 检查覆盖到的人员数量 |
| peopleRate | number | 人员覆盖率(%) | (覆盖人数 / 总人数) × 100 |

**业务意义**：反映安全检查的人员覆盖情况，覆盖率越高说明检查越全面。

### 2.3 设备检查指标

| 字段名称 | 类型 | 说明 | 计算方式 |
|---------|------|------|---------|
| checkRecorderCount | number | 检查记录仪台次 | 实际进行检查的记录仪使用次数 |
| activeRecorderCount | number | 投入使用台数 | 实际投入使用的记录仪数量 |
| totalRecorders | number | 视频记录仪数量 | 总的视频记录仪设备数 |
| recorderCheckRate | number | 记录仪检查率(%) | (检查台次 / 总台数) × 100 |

**业务意义**：评估设备使用效率和检查覆盖程度。

### 2.4 时间覆盖指标

| 字段名称 | 类型 | 说明 | 计算方式 |
|---------|------|------|---------|
| coverageTime | number | 检查覆盖时间(h) | 有效检查的总时长（小时） |
| timeRate | number | 时间覆盖率(%) | (检查时长 / 工作时长) × 100 |

**业务意义**：评估时间维度的检查覆盖情况，确保全天候监控。

### 2.5 问题统计指标

| 字段名称 | 类型 | 说明 | 数据来源 |
|---------|------|------|---------|
| issueCount | number | 问题数量 | 一般性问题的数量 |
| violationCount | number | 违章数量 | 违反操作规程的数量 |
| hazardCount | number | 隐患数量 | 安全隐患的数量 |

**业务意义**：
- **问题**：一般性的不规范操作或轻微违规
- **违章**：明确违反作业规程的行为
- **隐患**：可能导致事故的潜在危险

### 2.6 管理指标

| 字段名称 | 类型 | 说明 | 业务含义 |
|---------|------|------|---------|
| assessmentCount | number | 考核次数 | 对违规人员的考核处理次数 |
| remark | string | 备注 | 其他补充说明信息 |

---

## 三、页面功能详解

### 3.1 统计卡片区域

#### 功能说明
页面顶部展示4个核心指标的统计卡片：

1. **覆盖人数**
   - 图标：用户图标（蓝色）
   - 显示：最近一次统计的覆盖人数
   - 作用：快速了解检查覆盖的人员规模

2. **视频记录仪数量**
   - 图标：摄像机图标（绿色）
   - 显示：当前投入使用的记录仪总数
   - 作用：了解监控设备的部署情况

3. **问题总数**
   - 图标：警告图标（橙色）
   - 显示：问题+违章+隐患的总和
   - 作用：快速评估当前安全形势

4. **考核次数**
   - 图标：文档图标（红色）
   - 显示：总的考核处理次数
   - 作用：了解违规处理的执行力度

#### 交互效果
- 卡片悬停时有上浮动画
- 点击卡片可以快速筛选相关数据（可扩展）

### 3.2 搜索筛选区域

#### 筛选条件
1. **统计周期**
   - 选项：按日统计、按周统计、按月统计
   - 作用：查看不同粒度的统计数据

2. **时间范围**
   - 类型：日期范围选择器
   - 作用：查看指定时间段的档案

3. **部门**
   - 选项：采矿部、安全部、技术部等
   - 作用：查看特定部门的统计数据

#### 操作按钮
- **查询**：执行筛选查询
- **重置**：清空筛选条件
- **导出报表**：导出当前筛选结果为CSV文件

### 3.3 数据表格区域

#### 表格列说明

| 列名 | 宽度 | 说明 | 特殊展示 |
|-----|------|------|---------|
| 序号 | 60px | 行号 | 居中对齐 |
| 档案编号 | 160px | 唯一标识 | - |
| 统计周期 | 100px | 周期类型 | Tag标签（日/周/月） |
| 统计日期 | 180px | 统计基准日期 | - |
| 覆盖人数 | 100px | 人数 | - |
| 人员覆盖率 | 110px | 百分比 | 进度条（带颜色梯度） |
| 检查记录仪台次 | 140px | 台次 | - |
| 投入使用台数 | 120px | 台数 | - |
| 记录仪数量 | 110px | 总数 | - |
| 记录仪检查率 | 130px | 百分比 | 进度条（带颜色梯度） |
| 检查覆盖时间 | 150px | 小时 | - |
| 时间覆盖率 | 110px | 百分比 | 进度条（带颜色梯度） |
| 问题数量 | 100px | 数量 | 橙色Tag |
| 违章数量 | 100px | 数量 | 红色Tag |
| 隐患数量 | 100px | 数量 | 红色Tag |
| 考核次数 | 100px | 次数 | - |
| 操作 | 240px | 操作按钮 | 详情/编辑/删除 |

#### 进度条颜色规则
```typescript
- >= 80%：绿色（#67C23A）- 良好
- >= 60%：橙色（#E6A23C）- 中等
- < 60%：红色（#F56C6C）- 较差
```

#### 分页功能
- 每页条数：10/20/50/100 可选
- 支持跳转到指定页

### 3.4 新增/编辑对话框

#### 表单结构
对话框采用双列布局，字段分组：

**第1行**：
- 统计周期（下拉选择）
- 统计日期（日期选择）

**第2行**：
- 覆盖人数（数字输入）
- 人员覆盖率（数字输入，0-100）

**第3行**：
- 检查记录仪台次（数字输入）
- 投入使用台数（数字输入）

**第4行**：
- 视频记录仪数量（数字输入）
- 记录仪检查率（数字输入，0-100）

**第5行**：
- 检查覆盖时间（数字输入，支持小数）
- 时间覆盖率（数字输入，0-100）

**第6行**（三列）：
- 问题数量（数字输入）
- 违章数量（数字输入）
- 隐患数量（数字输入）

**第7行**：
- 考核次数（数字输入）
- 部门（下拉选择）

**第8行**：
- 备注（多行文本，500字限制）

#### 表单验证
- 统计周期：必填
- 统计日期：必填
- 覆盖人数：必填
- 人员覆盖率：必填
- 记录仪数量：必填

### 3.5 详情对话框

#### 展示内容
使用 `el-descriptions` 组件展示档案的完整信息，布局为2列。

#### 特殊显示
- 统计周期：Tag标签
- 人员覆盖率、记录仪检查率、时间覆盖率：进度条
- 覆盖人数：信息Tag
- 问题数量：警告Tag
- 违章数量、隐患数量：危险Tag

---

## 四、业务场景

### 场景1：日常统计归档
**角色**：安全管理员  
**操作流程**：
1. 每天下班前，打开报警档案模块
2. 点击"新增档案"按钮
3. 选择"按日统计"，填写当天的各项统计数据
4. 数据来源于监控预警模块的已归档记录
5. 保存档案记录

**数据来源**：
- 从监控预警模块统计已归档（status='archived'）的违规记录
- 从设备台账获取设备和人员信息
- 手动输入检查时长等辅助数据

### 场景2：周度/月度报告
**角色**：安全主管  
**操作流程**：
1. 每周一或每月初，打开报警档案模块
2. 选择"按周统计"或"按月统计"
3. 填写过去一周/一月的汇总数据
4. 填写备注说明（如：本周重点问题、改进措施等）
5. 导出报表提交给上级

**业务价值**：
- 形成规范的周报/月报
- 追踪安全指标的变化趋势
- 为管理决策提供数据支撑

### 场景3：部门对比分析
**角色**：安全总监  
**操作流程**：
1. 打开报警档案模块
2. 筛选条件设置为"最近30天"
3. 依次选择不同部门查看数据
4. 对比各部门的：
   - 人员覆盖率
   - 记录仪检查率
   - 违章数量
   - 考核次数
5. 导出数据进行进一步分析

**分析维度**：
- 哪个部门的检查覆盖最全面？
- 哪个部门的违规情况最严重？
- 哪个部门的整改执行最积极？

### 场景4：年度安全审计
**角色**：外部审计人员  
**操作流程**：
1. 打开报警档案模块
2. 筛选条件设置为"全年数据"
3. 查看详细的统计档案
4. 导出全年报表
5. 验证数据的完整性和准确性

**审计要点**：
- 检查是否有缺失的日期
- 验证统计数据的合理性
- 评估安全管理制度的执行情况

### 场景5：趋势分析
**角色**：数据分析师  
**操作流程**：
1. 导出近3个月的日统计数据
2. 使用Excel或BI工具分析：
   - 覆盖率的变化趋势
   - 违规数量的波动规律
   - 考核频次的分布
3. 制作可视化图表
4. 向管理层汇报分析结果

**分析目标**：
- 识别安全风险的高发时段
- 评估管理措施的有效性
- 预测未来的安全态势

---

## 五、与其他模块的关联

### 5.1 监控预警模块 → 报警档案模块

```
监控预警模块
  ↓
违规记录状态变更为"已归档"
  ↓
手动或自动触发统计
  ↓
生成档案记录
  ↓
报警档案模块展示
```

**数据流转**：
- 监控预警的已归档数据是报警档案的数据源
- 按照周期（日/周/月）汇总统计
- 形成结构化的档案记录

### 5.2 设备台账模块 → 报警档案模块

**关联数据**：
- 视频记录仪数量 = 设备台账中的设备总数
- 投入使用台数 = 设备台账中状态为"在线"的设备数
- 部门信息 = 设备台账中的部门分类

### 5.3 综合看板 ← 报警档案模块

**数据输出**：
- 报警档案的统计数据可以输出到综合看板
- 展示长期趋势和对比分析
- 提供决策支持

---

## 六、数据统计逻辑

### 6.1 日统计逻辑
```typescript
// 统计某一天的数据
function generateDailyArchive(date: string) {
  // 1. 获取该日期所有已归档的违规记录
  const violations = store.violations.filter(v => 
    v.status === 'archived' && 
    v.captureTime.startsWith(date)
  );
  
  // 2. 计算问题分类
  const issueCount = violations.filter(v => v.severity === 'low').length;
  const violationCount = violations.filter(v => 
    v.severity === 'medium' || v.severity === 'high'
  ).length;
  const hazardCount = violations.filter(v => v.severity === 'critical').length;
  
  // 3. 统计考核次数
  const assessmentCount = violations.filter(v => v.handler).length;
  
  // 4. 获取设备信息
  const totalRecorders = store.devices.length;
  const activeRecorders = store.devices.filter(d => d.status === 'online').length;
  
  // 5. 计算覆盖率（基于实际业务规则）
  const coveragePeople = calculateCoveragePeople(violations);
  const peopleRate = (coveragePeople / totalPeople) * 100;
  const recorderCheckRate = (checkRecorderCount / totalRecorders) * 100;
  
  // 6. 生成档案记录
  return {
    period: 'daily',
    statisticDate: date,
    issueCount,
    violationCount,
    hazardCount,
    assessmentCount,
    totalRecorders,
    activeRecorderCount: activeRecorders,
    coveragePeople,
    peopleRate,
    // ... 其他字段
  };
}
```

### 6.2 周统计逻辑
```typescript
// 统计某一周的数据
function generateWeeklyArchive(weekStartDate: string) {
  // 获取该周7天的日统计数据
  const dailyArchives = getDailyArchives(weekStartDate, 7);
  
  // 汇总计算
  return {
    period: 'weekly',
    statisticDate: weekStartDate,
    // 人数取平均值
    coveragePeople: average(dailyArchives.map(d => d.coveragePeople)),
    peopleRate: average(dailyArchives.map(d => d.peopleRate)),
    // 台次和次数取总和
    checkRecorderCount: sum(dailyArchives.map(d => d.checkRecorderCount)),
    issueCount: sum(dailyArchives.map(d => d.issueCount)),
    violationCount: sum(dailyArchives.map(d => d.violationCount)),
    hazardCount: sum(dailyArchives.map(d => d.hazardCount)),
    assessmentCount: sum(dailyArchives.map(d => d.assessmentCount)),
    // ... 其他字段
  };
}
```

### 6.3 月统计逻辑
```typescript
// 统计某一月的数据
function generateMonthlyArchive(month: string) {
  // 获取该月所有天的日统计数据
  const dailyArchives = getDailyArchivesInMonth(month);
  const days = dailyArchives.length;
  
  // 汇总计算
  return {
    period: 'monthly',
    statisticDate: month,
    // 人数和比率取平均值
    coveragePeople: average(dailyArchives.map(d => d.coveragePeople)),
    peopleRate: average(dailyArchives.map(d => d.peopleRate)),
    recorderCheckRate: average(dailyArchives.map(d => d.recorderCheckRate)),
    timeRate: average(dailyArchives.map(d => d.timeRate)),
    // 台次、数量、次数取总和
    checkRecorderCount: sum(dailyArchives.map(d => d.checkRecorderCount)),
    coverageTime: sum(dailyArchives.map(d => d.coverageTime)),
    issueCount: sum(dailyArchives.map(d => d.issueCount)),
    violationCount: sum(dailyArchives.map(d => d.violationCount)),
    hazardCount: sum(dailyArchives.map(d => d.hazardCount)),
    assessmentCount: sum(dailyArchives.map(d => d.assessmentCount)),
    // ... 其他字段
  };
}
```

---

## 七、关键指标说明

### 7.1 覆盖率指标体系

#### 人员覆盖率
```
人员覆盖率 = (被检查覆盖的人数 / 总在岗人数) × 100%

目标值：≥ 80%
警戒值：< 60%

业务含义：
- 反映安全检查在人员维度的全面性
- 确保每个作业人员都在监控范围内
- 避免监管盲区和漏洞
```

#### 记录仪检查率
```
记录仪检查率 = (实际检查台次 / 应检查台次) × 100%

其中：
- 实际检查台次 = 当天实际使用记录仪进行检查的次数
- 应检查台次 = 投入使用的记录仪数量 × 每天应检次数

目标值：≥ 90%
警戒值：< 70%

业务含义：
- 评估设备使用的充分性
- 反映检查工作的执行力度
- 避免设备闲置和检查懈怠
```

#### 时间覆盖率
```
时间覆盖率 = (有效检查时长 / 总工作时长) × 100%

其中：
- 有效检查时长 = 所有记录仪实际工作的总时长
- 总工作时长 = 工作班次时长 × 作业点数量

目标值：≥ 75%
警戒值：< 50%

业务含义：
- 评估时间维度的监控连续性
- 确保全天候的安全监管
- 识别监控的时间盲区
```

### 7.2 问题分类标准

#### 问题（Issue）
- **定义**：一般性的不规范操作或轻微违规
- **严重程度**：低（low）
- **示例**：
  - 工作服穿戴不整齐
  - 工作区域轻微杂乱
  - 非关键区域的轻微违规
- **处理方式**：提醒、教育

#### 违章（Violation）
- **定义**：明确违反作业规程的行为
- **严重程度**：中（medium）、高（high）
- **示例**：
  - 未佩戴安全帽
  - 未系安全带
  - 违规操作设备
- **处理方式**：警告、罚款、培训

#### 隐患（Hazard）
- **定义**：可能导致事故的潜在危险
- **严重程度**：严重（critical）
- **示例**：
  - 气瓶混放
  - 无证上岗
  - 通风不良
  - 未配灭火器
- **处理方式**：立即整改、停工、问责

---

## 八、最佳实践建议

### 8.1 数据录入规范

1. **及时性**
   - 日统计：每天下班前完成
   - 周统计：每周一上午完成
   - 月统计：每月3日前完成

2. **准确性**
   - 从监控预警模块导出已归档数据
   - 交叉验证设备台账的设备信息
   - 人工复核关键数据的合理性

3. **完整性**
   - 确保所有必填字段都有值
   - 重要数据补充详细备注
   - 异常数据说明原因

### 8.2 数据分析建议

1. **纵向对比**
   - 对比本周与上周的数据
   - 对比本月与上月的数据
   - 识别变化趋势和异常波动

2. **横向对比**
   - 对比不同部门的数据
   - 识别优秀部门的经验
   - 帮助落后部门改进

3. **关联分析**
   - 覆盖率与违规数量的关系
   - 检查频次与隐患发现的关系
   - 考核力度与违规减少的关系

### 8.3 报表使用建议

1. **日常管理**
   - 每日查看最新统计
   - 关注覆盖率指标
   - 及时发现问题趋势

2. **周例会**
   - 展示本周统计数据
   - 对比各部门表现
   - 制定下周改进措施

3. **月度汇报**
   - 导出月度报表
   - 制作趋势图表
   - 向管理层汇报

4. **年度总结**
   - 汇总全年数据
   - 分析年度趋势
   - 评估安全管理成效

---

## 九、技术实现要点

### 9.1 数据模拟
当前版本使用前端模拟数据，生成近30天的日统计记录：
- 覆盖率在75%-95%之间随机
- 违规数量符合实际分布
- 部门轮换生成数据

### 9.2 数据导出
支持CSV格式导出，包含所有字段：
- 使用UTF-8编码（带BOM）
- 支持Excel直接打开
- 百分比字段带"%"符号

### 9.3 性能优化
- 大数据量时使用虚拟滚动
- 分页加载减少初始渲染
- 计算属性缓存统计结果

### 9.4 未来扩展
1. **自动统计**
   - 定时任务自动生成日统计
   - 从监控预警模块自动抓取数据
   - 自动计算各项指标

2. **智能分析**
   - AI预测未来趋势
   - 自动识别异常数据
   - 智能推荐改进措施

3. **可视化报表**
   - 集成ECharts图表
   - 多维度数据展示
   - 交互式数据钻取

---

## 十、总结

报警档案模块是铜矿AI安全监控系统的重要组成部分，实现了从实时监控到历史统计的闭环管理。通过规范的数据归档和多维度的统计分析，为安全管理提供了科学的决策依据。

**核心价值**：
- 📊 系统化的数据归档
- 📈 多维度的统计分析
- 📑 规范化的报表输出
- 🎯 科学化的决策支撑

**使用建议**：
- 坚持daily数据录入
- 定期导出备份
- 结合其他模块使用
- 持续优化统计维度

