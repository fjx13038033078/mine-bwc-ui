# 监控预警模块 - 功能说明

## 一、模块概述

### 1.1 模块定位
**监控预警模块**是铜矿AI安全监控系统的核心业务模块，负责实时展示AI检测到的违规行为，并提供完整的违规处理流程，从发现、上报、处理到归档形成闭环管理。

### 1.2 文件路径
`/src/views/violation-monitor/index.vue`

### 1.3 核心价值
- 🚨 **实时预警**：第一时间发现和展示违规行为
- 📊 **全局监控**：统一管理所有违规数据
- 🔄 **闭环处理**：完整的处理流程和记录
- 📈 **数据分析**：多维度统计分析违规情况

---

## 二、业务场景

### 2.1 实际工作流程

```
AI识别违规 → 监控预警系统展示 → 安全员查看详情 → 判断真实性
    ↓                                                      ↓
处理记录    ← 归档留存 ← 处理完成 ← 现场整改 ← 分配处理人/上报
```

### 2.2 典型使用场景

#### 场景1：日常违规监控
**角色**：安全监督员  
**流程**：
1. 每天上班后打开监控预警系统
2. 查看"待处理"违规列表
3. 逐条查看违规详情和视频回放
4. 判断是否为真实违规
5. 对真实违规进行处理分配

#### 场景2：重大违规上报
**角色**：安全部门负责人  
**流程**：
1. 发现严重程度为"重大"的违规
2. 查看详情确认情况
3. 点击"上报"选择上报部门
4. 填写上报等级和说明
5. 提交上报给公司领导层

#### 场景3：违规处理跟踪
**角色**：违规处理人  
**流程**：
1. 接收到违规处理任务
2. 到现场核实情况
3. 要求作业人员整改
4. 在系统中填写处理措施和结果
5. 提交处理记录

#### 场景4：误报标记
**角色**：安全监督员  
**流程**：
1. 查看违规详情
2. 播放视频回放核实
3. 发现AI识别错误
4. 点击"标记误报"
5. 系统记录误报情况

#### 场景5：月度数据归档
**角色**：系统管理员  
**流程**：
1. 筛选上个月已处理的违规
2. 批量选择记录
3. 点击"批量归档"
4. 系统将记录归档保存

---

## 三、核心功能

### 3.1 违规展示

#### 📋 违规列表
**展示信息**：
- **违规截图**（新增）：直观展示违规现场画面
  - 点击可放大查看
  - 有视频的记录显示播放按钮
  - 无截图时显示占位提示
- 违规ID（唯一标识）
- 识别时间
- 违规类型（未佩戴安全帽、气瓶平放等）
- 严重程度（轻微/一般/严重/重大）
- 设备信息（设备ID、部门、责任人）
- 违规描述
- 处理状态
- 是否上报
- 处理人

**截图/视频功能**：
- **列表缩略图**：每条违规记录显示100x70px的截图
- **播放按钮**：有视频的记录显示播放图标，点击播放视频
- **图片预览**：点击截图可以放大预览
- **加载失败处理**：图片加载失败时显示友好提示

**排序规则**：
- 默认按识别时间倒序
- 待处理的优先显示
- 重大违规优先显示

#### 🔍 多维度筛选
**筛选条件**：
- 违规类型（8种常见违规）
- 严重程度（4个等级）
- 处理状态（5种状态）
- 所属部门（5个部门）
- 识别时间范围

#### 📊 实时统计
**统计卡片（6个）**：
1. **待处理**：需要立即关注的违规数量
2. **处理中**：正在处理的违规数量
3. **已处理**：处理完成的违规数量
4. **已归档**：归档保存的违规数量
5. **误报**：AI误报的数量
6. **总计**：所有违规总数

---

### 3.2 违规处理

#### 1️⃣ 上报功能
**适用场景**：
- 严重违规需要上级关注
- 多次违规需要通报
- 重大安全隐患

**上报选项**：
- **上报部门**：安全管理部、生产调度部、矿区管理处、公司领导层
- **上报等级**：
  - 一般上报：常规违规通报
  - 紧急上报：需要立即处理的违规
  - 重大隐患：可能造成严重后果的违规
- **上报说明**：详细说明上报原因和情况

**上报效果**：
- 系统标记为"已上报"
- 添加上报记录到处理历史
- 可以批量上报多条违规

#### 2️⃣ 处理功能
**处理流程**：
```
选择违规 → 点击处理 → 填写处理信息 → 提交
```

**处理信息**：
- **处理状态**：
  - 处理中：已开始处理但未完成
  - 已处理：处理完成
- **处理人**：负责处理的人员姓名
- **处理措施**：采取的具体措施（必填）
- **处理结果**：处理后的结果（必填）
- **备注**：其他需要说明的内容

**处理记录**：
- 自动记录处理时间
- 记录处理人
- 记录处理措施和结果
- 形成完整的处理历史

#### 3️⃣ 归档功能
**归档条件**：
- 只能归档"已处理"状态的违规
- 归档后不可再修改

**归档作用**：
- 整理已完成的违规记录
- 减少当前工作列表
- 保留历史数据便于分析

**归档操作**：
- 单条归档：点击归档按钮
- 批量归档：选择多条后批量操作

#### 4️⃣ 误报标记
**标记条件**：
- 只能标记"待处理"状态的违规
- 经过视频回放核实

**标记原因**：
- AI识别错误
- 画面遮挡导致误判
- 光线问题导致误判
- 其他非真实违规情况

**标记效果**：
- 状态改为"误报"
- 添加误报记录
- 可用于AI模型优化参考

---

### 3.3 违规详情

#### 🎬 违规视频/截图（新增）
**左侧视频播放区**：
- 有视频时显示视频播放器，支持播放控制
- 无视频时显示高清截图
- 标注违规时刻（如：00:00:35）
- 显示AI识别置信度
- 视频尺寸：自适应，最大高度400px

**右侧截图展示区**：
- 显示违规瞬间的高清截图
- 点击可放大预览
- 截图尺寸：350px高度
- 提示"点击图片可放大查看"

**媒体信息**：
- 可播放状态标识
- 违规时刻标注
- 识别置信度显示

#### 📄 基本信息
- 违规ID、识别时间
- 违规类型、严重程度
- 设备ID、视频ID（可点击跳转）
- 所属部门、责任人
- 处理状态、是否上报
- 处理人、处理时间
- 违规描述

#### 📖 关联规范
**展示内容**：
- 规范类型（焊割作业、吊装作业等）
- 违规行为描述
- 规范要求说明

**数据来源**：从安全规范库匹配

#### 💡 处理建议
**展示内容**：
- 处理措施：建议采取的处理措施
- 处罚依据：相关的管理条例
- 预防措施：防止再次发生的措施

**数据来源**：从违规处理知识库匹配

#### 📝 处理记录
**时间线展示**：
- AI自动识别记录
- 开始处理记录
- 处理完成记录
- 上报记录
- 归档记录

**记录内容**：
- 操作时间
- 操作人
- 操作类型
- 备注说明

---

### 3.4 视频回放

#### 🎬 播放功能
- 在线播放违规视频
- 支持播放控制（播放、暂停、进度条）
- 显示视频基本信息

#### 📹 视频信息
- 视频ID
- 采集时间
- 设备ID
- 视频时长

**作用**：
- 核实违规真实性
- 判断违规严重程度
- 作为处理依据

---

## 四、数据结构

### 4.1 违规数据模型

```typescript
interface ViolationVO {
  violationId: string;              // 违规ID
  videoId: string;                  // 关联视频ID
  deviceId: string;                 // 设备ID
  captureTime: string;              // 识别时间
  violationType: string;            // 违规类型
  severity: 'low' | 'medium' | 'high' | 'critical';  // 严重程度
  description: string;              // 违规描述
  department: string;               // 所属部门
  principal: string;                // 责任人
  status: 'pending' | 'processing' | 'handled' | 'archived' | 'false_alarm';  // 处理状态
  reported: boolean;                // 是否上报
  handler?: string;                 // 处理人
  handleTime?: string;              // 处理时间
  handleRecords?: HandleRecord[];   // 处理记录
  matchedRegulation?: {             // 关联规范
    workType: string;
    violation: string;
    standard: string;
  };
  handlingSuggestion?: {            // 处理建议
    measure: string;
    basis: string;
    prevention: string;
  };
}
```

### 4.2 处理记录模型

```typescript
interface HandleRecord {
  time: string;           // 操作时间
  operator: string;       // 操作人
  action: string;         // 操作类型
  remark?: string;        // 备注说明
}
```

### 4.3 状态流转

```
pending (待处理)
    ↓
processing (处理中)
    ↓
handled (已处理)
    ↓
archived (已归档)

pending (待处理)
    ↓
false_alarm (误报)
```

---

## 五、与其他模块的关联

### 5.1 数据流转关系

```
视频管理模块 (AI识别结果)
    ↓
监控预警模块 (违规展示和处理)
    ↓
设备台账 (设备信息关联)
    ↓
安全规范库 (违规规范匹配)
    ↓
知识库 (处理建议匹配)
    ↓
文档库 (处罚依据查询)
```

### 5.2 具体关联说明

#### 🔗 关联1：视频管理 → 监控预警
**数据流向**：AI检测到的违规数据展示到监控预警

**关联方式**：
```typescript
// 从视频管理的AI检测结果生成违规记录
interface AIDetectionResult {
  videoId: string;
  deviceId: string;
  detectionTime: string;
  violations: {
    type: string;
    severity: string;
    confidence: number;
    description: string;
  }[];
}

// 转换为违规记录
function createViolationFromDetection(detection: AIDetectionResult) {
  return {
    violationId: generateId('VIO'),
    videoId: detection.videoId,
    deviceId: detection.deviceId,
    captureTime: detection.detectionTime,
    violationType: detection.violations[0].type,
    severity: detection.violations[0].severity,
    description: detection.violations[0].description,
    status: 'pending',
    reported: false
  };
}
```

#### 🔗 关联2：监控预警 → 设备台账
**数据流向**：获取设备的部门和责任人信息

**关联方式**：
```typescript
// 通过设备ID获取设备信息
function getDeviceInfo(deviceId: string) {
  const device = deviceList.find(d => d.deviceId === deviceId);
  return {
    department: device.department,
    principal: device.principal
  };
}
```

#### 🔗 关联3：监控预警 → 安全规范库
**数据流向**：根据违规类型匹配相关规范

**关联方式**：
```typescript
// 根据违规类型匹配规范
function matchRegulation(violationType: string) {
  // 在规范库中搜索包含该违规类型的规范
  return regulationList.find(r => 
    r.violations.includes(violationType)
  );
}
```

#### 🔗 关联4：监控预警 → 知识库
**数据流向**：根据违规类型获取处理建议

**关联方式**：
```typescript
// 根据违规类型获取处理建议
function getHandlingSuggestion(violationType: string) {
  return knowledgeBase.find(k => 
    k.violationType === violationType
  );
}
```

---

## 六、操作指南

### 6.1 日常监控操作

#### Step 1：查看待处理违规
1. 进入监控预警页面
2. 查看统计卡片中的"待处理"数量
3. 筛选条件选择"处理状态 = 待处理"
4. 点击搜索

#### Step 2：快速核实（新增）
1. 直接查看列表中的违规截图
2. 点击播放按钮快速观看视频片段
3. 通过缩略图快速判断违规真实性

#### Step 3：查看详情
1. 点击违规记录的"查看详情"按钮
2. 左侧查看违规视频/截图（**大屏展示**）
3. 右侧查看高清截图
4. 查看违规时刻和识别置信度
5. 查看违规基本信息
6. 查看关联规范和处理建议

#### Step 4：判断真实性
- **如果是真实违规** → 进行处理分配
- **如果是误报** → 标记误报（可能是光线、遮挡等原因）

### 6.2 违规处理操作

#### 情况1：一般违规处理
```
1. 点击"处理"按钮
2. 选择处理状态：处理中/已处理
3. 填写处理人姓名
4. 填写处理措施（必填）：
   "已通知现场负责人张三，要求作业人员立即佩戴安全帽，
    并对责任人进行安全教育"
5. 填写处理结果（必填）：
   "违规人员已整改，重新佩戴安全帽，接受安全教育"
6. 点击"提交处理"
```

#### 情况2：严重违规上报
```
1. 点击"上报"按钮
2. 选择上报部门：安全管理部
3. 选择上报等级：紧急上报
4. 填写上报说明：
   "井下作业区发现严重违规，作业人员未佩戴安全帽进入危险区域，
    存在重大安全隐患，请立即处理"
5. 点击"确认上报"
6. 再点击"处理"进行处理分配
```

#### 情况3：误报标记
```
1. 点击"播放视频"核实情况
2. 确认为AI误判
3. 点击"标记误报"
4. 在确认对话框中点击"确定"
5. 系统自动记录误报情况
```

### 6.3 批量操作

#### 批量上报
```
1. 勾选需要上报的多条违规记录
2. 点击顶部"批量上报"按钮
3. 填写上报信息
4. 提交
```

#### 批量处理
```
1. 勾选需要处理的多条违规记录
2. 点击顶部"批量处理"按钮
3. 确认批量处理
4. 系统自动标记为"已处理"
```

#### 批量归档
```
1. 筛选"已处理"状态的违规
2. 勾选需要归档的记录
3. 点击顶部"批量归档"按钮
4. 确认归档
```

### 6.4 数据导出

```
1. 设置筛选条件（可选）
2. 点击"导出报表"按钮
3. 系统导出CSV文件
4. 文件包含所有违规记录的完整信息
```

**导出用途**：
- 月度安全报告
- 部门考核依据
- 趋势分析
- 领导汇报

---

## 七、统计分析

### 7.1 实时统计卡片

| 统计项 | 说明 | 用途 |
|--------|------|------|
| 待处理 | 需要立即处理的违规数 | 工作量提示 |
| 处理中 | 正在处理的违规数 | 进度跟踪 |
| 已处理 | 处理完成的违规数 | 工作成果 |
| 已归档 | 归档保存的违规数 | 历史数据 |
| 误报 | AI误报的数量 | 模型优化参考 |
| 总计 | 所有违规总数 | 整体规模 |

### 7.2 数据分析维度

#### 按违规类型分析
- 哪种违规最多
- 重点监控的违规类型
- 违规类型趋势

#### 按严重程度分析
- 重大违规数量
- 严重违规占比
- 安全形势评估

#### 按部门分析
- 各部门违规数量
- 部门安全排名
- 部门考核依据

#### 按时间分析
- 每天违规数量
- 每周违规趋势
- 每月违规统计

#### 按处理效率分析
- 平均处理时长
- 处理完成率
- 上报率

---

## 八、最佳实践

### 8.1 日常工作建议

#### ✅ 每日必做
1. **早上**：查看待处理违规，处理优先级
2. **上午**：处理重大和严重违规
3. **下午**：处理一般违规
4. **下班前**：检查当天处理情况

#### ✅ 每周工作
1. **周一**：查看上周违规统计
2. **周三**：跟进处理中的违规
3. **周五**：归档已处理的违规
4. **周总结**：分析本周违规趋势

#### ✅ 每月工作
1. **月初**：上月数据归档
2. **月中**：月度数据分析
3. **月末**：准备月度报告
4. **月总结**：部门安全评估

### 8.2 处理规范

#### 📋 处理标准
1. **及时性**：
   - 重大违规：立即处理（30分钟内）
   - 严重违规：当天处理
   - 一般违规：3天内处理
   - 轻微违规：7天内处理

2. **完整性**：
   - 处理措施要具体明确
   - 处理结果要真实可查
   - 处理记录要完整保存

3. **准确性**：
   - 判断违规真实性
   - 评估违规严重程度
   - 标记误报要慎重

#### 📋 上报标准
**必须上报**：
- 重大违规
- 连续多次违规
- 造成事故的违规
- 拒不整改的违规

**建议上报**：
- 严重违规
- 典型违规案例
- 需要通报的违规

**无需上报**：
- 轻微违规
- 首次违规且已整改
- 误报

### 8.3 注意事项

#### ⚠️ 操作注意
1. **核实再处理**：通过视频回放核实违规真实性
2. **记录要详细**：处理措施和结果要详细记录
3. **及时跟进**：处理中的违规要及时跟进
4. **慎重归档**：归档前确认处理完成

#### ⚠️ 数据安全
1. **不可删除**：违规记录不能删除，只能归档
2. **不可篡改**：处理记录不能修改
3. **权限控制**：只有授权人员可以处理
4. **数据备份**：定期备份违规数据

---

## 九、技术实现

### 9.1 数据获取

```typescript
// 从视频管理模块获取违规数据
function getViolationsFromVideos() {
  const violations: ViolationVO[] = [];
  
  // 遍历所有视频记录
  videoList.forEach(video => {
    // 如果AI检测状态为已完成且有违规
    if (video.aiStatus === 'completed' && video.aiResult) {
      video.aiResult.violations.forEach(v => {
        // 创建违规记录
        const violation: ViolationVO = {
          violationId: generateId('VIO'),
          videoId: video.videoId,
          deviceId: video.deviceId,
          captureTime: video.captureTime,
          violationType: v.type,
          severity: v.severity,
          description: v.description,
          department: getDeviceInfo(video.deviceId).department,
          principal: getDeviceInfo(video.deviceId).principal,
          status: 'pending',
          reported: false,
          matchedRegulation: matchRegulation(v.type),
          handlingSuggestion: getHandlingSuggestion(v.type)
        };
        
        violations.push(violation);
      });
    }
  });
  
  return violations;
}
```

### 9.2 状态管理

```typescript
// 更新违规状态
function updateViolationStatus(
  violationId: string, 
  newStatus: string,
  operator: string,
  remark?: string
) {
  const violation = violations.find(v => v.violationId === violationId);
  if (violation) {
    violation.status = newStatus;
    
    // 添加处理记录
    violation.handleRecords.push({
      time: formatDateTime(),
      operator,
      action: getActionName(newStatus),
      remark
    });
  }
}
```

### 9.3 实时刷新

```typescript
// 实时监听新违规
function watchNewViolations() {
  // 轮询或WebSocket监听新违规
  setInterval(() => {
    const newViolations = getViolationsFromVideos();
    
    // 对比并添加新违规
    newViolations.forEach(nv => {
      const exists = violations.find(v => v.violationId === nv.violationId);
      if (!exists) {
        violations.unshift(nv);
        
        // 发送通知
        sendNotification(`发现新违规：${nv.violationType}`);
      }
    });
  }, 60000); // 每分钟检查一次
}
```

---

## 十、扩展功能

### 10.1 短期扩展
- [ ] 违规预警推送（钉钉、企业微信）
- [ ] 处理进度提醒
- [ ] 违规趋势图表
- [ ] 部门违规排名

### 10.2 中期扩展
- [ ] 违规处理工作流
- [ ] 违规处理审批
- [ ] 处理效果评估
- [ ] 违规热力图

### 10.3 长期扩展
- [ ] 智能处理建议
- [ ] 违规预测分析
- [ ] 跨矿区数据对比
- [ ] 安全评分系统

---

## 十一、总结

### 核心价值
1. ✅ **实时监控**：第一时间发现违规
2. ✅ **闭环管理**：完整的处理流程
3. ✅ **数据留存**：完整的处理记录
4. ✅ **决策支持**：多维度数据分析

### 业务价值
1. ✅ **提升效率**：统一平台管理所有违规
2. ✅ **降低风险**：及时处理安全隐患
3. ✅ **责任到人**：明确处理责任
4. ✅ **数据支撑**：为安全管理提供依据

### 管理价值
1. ✅ **全局掌控**：领导随时了解安全状况
2. ✅ **考核依据**：部门和个人考核数据
3. ✅ **持续改进**：基于数据优化管理
4. ✅ **合规留痕**：完整的记录符合监管要求

---

**文档版本**：v1.0  
**创建时间**：2024-11-21  
**维护部门**：安全管理部 + 技术研发部  
**关联模块**：视频管理、设备台账、安全规范库、知识库

---

**铜矿安全，从监控预警开始！**

