# 大模型AI技能启停管理模块 - 功能说明

## 一、模块概述

### 1.1 模块定位
**大模型AI技能启停管理模块**是铜矿AI安全监控系统的核心控制中心，负责管理所有AI识别技能的启用/停用状态，是连接设备配置、视频识别、规范库和知识库的关键枢纽。

### 1.2 文件路径
`/src/views/skillsManagement/ai-skills/index.vue`

### 1.3 核心价值
- 🎯 **集中管理**：统一管理所有AI识别技能
- 🔄 **动态控制**：灵活启用/停用技能
- 📊 **数据透明**：实时展示技能使用情况
- 🔗 **业务打通**：与其他模块数据流转

---

## 二、核心功能

### 2.1 技能状态管理

#### ✅ 查看所有技能状态
- 展示所有技能及其启用/停用状态
- 彩色标签区分：启用（绿色）/ 停用（灰色）
- 图标+名称直观展示
- 支持按名称、类别、状态筛选

#### ✅ 启用/停用单个技能
- 点击操作按钮快速切换状态
- 启用后设备可配置该技能
- 停用后设备无法新增配置（已配置设备不受影响）
- 操作前二次确认，防止误操作

#### ✅ 批量启用/停用
- 支持选择多个技能批量操作
- 批量启用：一键开启多个技能
- 批量停用：批量关闭不需要的技能
- 操作记录自动更新时间

---

### 2.2 技能详情查看

#### 📋 基础信息
- **技能ID**：唯一标识
- **技能名称**：如"违规行为识别"、"焊割作业规范识别"
- **技能描述**：功能简述
- **技能类别**：安全监控 / 专项作业
- **适用作业**：焊割作业、吊装作业、高处作业等

#### 📊 使用统计
- **设备使用数**：当前有多少设备配置了该技能
- **识别次数**：该技能累计识别次数
- **违规识别数**：识别出的违规行为数量
- **创建/更新时间**：技能创建和最后修改时间

---

### 2.3 关联知识库查看

#### 📚 安全作业规范关联
查看该技能关联的安全规范：
- 规范ID
- 作业类型
- 规范要求
- 常见违规行为

**业务意义**：AI识别时会参考这些规范进行判定

#### 📖 违规处理知识关联
查看该技能关联的处理知识：
- 知识ID
- 违规行为名称
- 严重等级
- 立即处理措施

**业务意义**：识别到违规后，系统会提供相应的处理建议

---

### 2.4 使用设备查看

#### 🖥️ 设备列表展示
查看配置了该技能的所有设备：
- 设备ID、序列号、型号
- 归属部门、责任人
- 设备状态（在线/离线/维护中/故障）
- 视频采集数、违规识别数

**业务意义**：了解技能的实际使用情况和效果

---

## 三、数据结构

### 3.1 技能数据模型

```typescript
interface AISkillVO {
  skillId: string;              // 技能ID，如：violation_detection
  skillName: string;            // 技能名称，如：违规行为识别
  description: string;          // 功能描述
  category: string;             // 类别：安全监控/专项作业
  icon: string;                 // 图标名称
  color: string;                // 显示颜色
  status: 'enabled' | 'disabled';  // 状态：启用/停用
  workTypes: string[];          // 适用作业类型
  deviceUsageCount: number;     // 设备使用数
  detectionCount: number;       // 识别次数
  violationCount: number;       // 违规识别数
  createTime: string;           // 创建时间
  updateTime: string;           // 更新时间
}
```

### 3.2 8种AI技能定义

| 技能ID | 技能名称 | 类别 | 适用作业 |
|--------|---------|------|---------|
| violation_detection | 违规行为识别 | 安全监控 | 通用作业 |
| welding_standard | 焊割作业规范识别 | 专项作业 | 焊割作业、动火作业 |
| lifting_standard | 吊装作业规范识别 | 专项作业 | 吊装作业 |
| ppe_detection | 个人防护装备检测 | 安全监控 | 通用作业 |
| confined_space | 受限空间作业监控 | 专项作业 | 受限空间作业 |
| high_altitude | 高处作业安全监控 | 专项作业 | 高处作业 |
| fire_hazard | 动火作业风险识别 | 专项作业 | 动火作业、焊割作业 |
| electrical_safety | 电气作业安全识别 | 专项作业 | 电气作业 |

---

## 四、与其他模块的数据流转

### 4.1 整体数据流向图

```
┌─────────────────────────────────────────────────────────────┐
│                    AI技能启停管理模块                        │
│                    (技能状态控制中心)                        │
└─────────────────┬───────────────────────┬───────────────────┘
                  │                       │
        ┌─────────▼─────────┐   ┌────────▼──────────┐
        │   设备台账管理    │   │   技能配置管理    │
        │   (设备信息)      │   │ (设备-技能绑定)   │
        └─────────┬─────────┘   └────────┬──────────┘
                  │                       │
                  └───────────┬───────────┘
                              │
                    ┌─────────▼──────────┐
                    │   视频管理与回放    │
                    │   (AI识别执行)     │
                    └─────────┬──────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
      ┌─────────▼────┐ ┌─────▼──────┐ ┌──▼──────────┐
      │ 安全规范库   │ │ 违规知识库 │ │ 文件库      │
      │ (判定依据)   │ │ (处理建议) │ │ (制度支撑)  │
      └──────────────┘ └────────────┘ └─────────────┘
```

---

### 4.2 具体数据流转关系

#### 📊 流转1：技能启停 → 技能配置

**数据流向**：AI技能启停管理 → 技能配置模块

**流转内容**：
- 技能状态（enabled/disabled）
- 技能基础信息（ID、名称、描述）
- 适用作业类型

**业务逻辑**：
```
1. 在【AI技能启停管理】中启用某个技能
   ↓
2. 该技能出现在【技能配置】模块的可选技能列表中
   ↓
3. 管理员可以为设备配置该技能
   ↓
4. 如果技能被停用，则【技能配置】中不再显示（已配置设备不受影响）
```

**代码示例**：
```typescript
// 技能配置模块获取可用技能
const availableSkills = allSkills.filter(skill => skill.status === 'enabled');
```

---

#### 📊 流转2：技能配置 → 设备台账

**数据流向**：技能配置模块 → 设备台账管理

**流转内容**：
- 设备已配置的技能列表
- 技能数量统计

**业务逻辑**：
```
1. 在【技能配置】中为设备绑定技能
   ↓
2. 【设备台账】中显示该设备已配置的技能数量
   ↓
3. 【AI技能启停管理】中统计该技能被多少设备使用
```

**数据关联**：
```typescript
// 设备数据包含配置的技能ID列表
interface DeviceVO {
  deviceId: string;
  configuredSkills: string[];  // 已配置的技能ID列表
  // ... 其他字段
}

// 统计技能使用设备数
const deviceUsageCount = devices.filter(device => 
  device.configuredSkills.includes(skillId)
).length;
```

---

#### 📊 流转3：设备配置 → 视频识别

**数据流向**：设备台账 + 技能配置 → 视频管理与回放

**流转内容**：
- 设备信息
- 设备已配置的技能列表
- 技能识别规则

**业务逻辑**：
```
1. 设备采集视频
   ↓
2. 系统读取该设备已配置的技能列表
   ↓
3. 根据技能列表进行AI识别
   ↓
4. 识别结果存储在【视频管理】中
   ↓
5. 【AI技能启停管理】中更新识别次数和违规数统计
```

**AI识别流程**：
```typescript
// 视频AI识别
function analyzeVideo(videoId: string, deviceId: string) {
  // 1. 获取设备配置的技能
  const device = getDevice(deviceId);
  const skills = device.configuredSkills;
  
  // 2. 根据技能进行识别
  const results = skills.map(skillId => {
    const skill = getSkill(skillId);
    if (skill.status === 'enabled') {
      return performDetection(videoId, skill);
    }
  });
  
  // 3. 更新统计数据
  updateSkillStats(skills, results);
}
```

---

#### 📊 流转4：视频识别 → 规范库和知识库

**数据流向**：视频管理 → 安全规范库 + 违规处理知识库

**流转内容**：
- 识别到的违规行为
- 违规类型和严重程度
- 对应的规范条目
- 处理建议

**业务逻辑**：
```
1. AI识别到违规行为
   ↓
2. 根据技能ID和违规类型，查询【安全规范库】
   ↓
3. 匹配对应的规范条目（该违规违反了哪条规范）
   ↓
4. 查询【违规处理知识库】
   ↓
5. 获取处理建议（立即措施、处罚、整改要求）
   ↓
6. 在视频详情中展示完整信息
```

**数据匹配示例**：
```typescript
// 违规识别结果
interface ViolationResult {
  violationType: string;        // 违规类型：未佩戴安全帽
  skillId: string;              // 识别技能：ppe_detection
  confidence: number;           // 置信度：0.95
  timestamp: string;            // 时间点：00:01:23
}

// 匹配规范
const regulation = regulations.find(r => 
  r.workType === skill.workTypes[0] && 
  r.violations.includes(violationType)
);

// 匹配处理知识
const handling = handlingKnowledge.find(h => 
  h.violationName === violationType
);
```

---

#### 📊 流转5：统计数据回流

**数据流向**：各业务模块 → AI技能启停管理

**流转内容**：
- 设备使用数据
- 识别次数统计
- 违规识别数据

**业务逻辑**：
```
设备台账：提供设备总数和配置情况
   ↓
技能配置：统计每个技能被多少设备使用
   ↓
视频管理：统计识别次数和违规数
   ↓
【AI技能启停管理】汇总展示所有统计数据
```

**统计计算**：
```typescript
// 计算技能使用统计
function calculateSkillStats(skillId: string) {
  // 1. 统计使用该技能的设备数
  const deviceUsageCount = devices.filter(d => 
    d.configuredSkills.includes(skillId) && 
    d.status === 'online'
  ).length;
  
  // 2. 统计识别次数
  const detectionCount = videos.filter(v => 
    v.detectedBy === skillId
  ).length;
  
  // 3. 统计违规识别数
  const violationCount = videos.reduce((sum, v) => 
    sum + v.violations.filter(vio => vio.skillId === skillId).length, 
    0
  );
  
  return { deviceUsageCount, detectionCount, violationCount };
}
```

---

## 五、业务场景示例

### 场景1：新技能上线

```
1. 研发团队开发了新的AI技能"电气作业安全识别"
   ↓
2. 在【AI技能启停管理】中，该技能默认为"停用"状态
   ↓
3. 管理员测试通过后，点击"启用"按钮
   ↓
4. 技能状态变为"启用"，更新时间记录
   ↓
5. 【技能配置】模块中该技能出现在可选列表
   ↓
6. 管理员为电气作业区域的设备配置该技能
   ↓
7. 设备开始使用该技能进行AI识别
   ↓
8. 【AI技能启停管理】中实时显示使用数据
```

---

### 场景2：技能效果评估

```
1. 管理员在【AI技能启停管理】中查看"焊割作业规范识别"
   ↓
2. 点击"详情"查看统计数据：
   - 设备使用数：12台
   - 识别次数：856次
   - 违规识别数：45次
   ↓
3. 点击"使用设备"查看哪些设备在使用
   ↓
4. 点击"关联知识库"查看识别依据
   ↓
5. 评估技能效果，决定是否需要优化
```

---

### 场景3：临时停用技能

```
1. 某技能识别准确率不高，需要优化
   ↓
2. 管理员在【AI技能启停管理】中点击"停用"
   ↓
3. 系统提示：已配置设备不受影响，但不再允许新设备配置
   ↓
4. 确认停用后，技能状态变为"停用"
   ↓
5. 【技能配置】中该技能不再显示在可选列表
   ↓
6. 已配置设备仍可使用该技能（保证业务连续性）
   ↓
7. 技术团队优化完成后，再次启用
```

---

### 场景4：批量管理技能

```
1. 新矿区开工，需要启用多个作业相关技能
   ↓
2. 管理员在【AI技能启停管理】中选择：
   - 焊割作业规范识别
   - 吊装作业规范识别
   - 高处作业安全监控
   - 个人防护装备检测
   ↓
3. 点击"批量启用"
   ↓
4. 系统一次性启用所有选中技能
   ↓
5. 管理员到【技能配置】为新设备批量配置这些技能
```

---

## 六、统计卡片说明

### 📊 统计卡片1：技能总数
- **数据来源**：系统中所有技能数量
- **计算方式**：`allSkills.length`
- **业务意义**：了解系统技能规模

### 📊 统计卡片2：已启用
- **数据来源**：状态为"enabled"的技能数量
- **计算方式**：`allSkills.filter(s => s.status === 'enabled').length`
- **业务意义**：当前可用的技能数量

### 📊 统计卡片3：已停用
- **数据来源**：状态为"disabled"的技能数量
- **计算方式**：`allSkills.filter(s => s.status === 'disabled').length`
- **业务意义**：暂时不可用的技能数量

### 📊 统计卡片4：设备使用次数
- **数据来源**：所有技能的设备使用数累加
- **计算方式**：`allSkills.reduce((sum, s) => sum + s.deviceUsageCount, 0)`
- **业务意义**：技能在设备上的总体部署规模

---

## 七、界面功能说明

### 7.1 搜索筛选
- ✅ 按技能名称模糊搜索
- ✅ 按技能类别筛选（安全监控/专项作业）
- ✅ 按技能状态筛选（启用/停用）
- ✅ 支持组合筛选
- ✅ 重置功能快速清空条件

### 7.2 表格展示
- ✅ 多选框支持批量操作
- ✅ 技能图标+名称直观展示
- ✅ 状态彩色标签
- ✅ 使用统计数据实时展示
- ✅ 操作按钮tooltip提示

### 7.3 操作按钮
| 按钮 | 图标 | 功能 | 权限控制 |
|------|------|------|---------|
| 详情 | View | 查看技能完整信息 | 所有人 |
| 启用/停用 | VideoPlay/VideoPause | 切换技能状态 | 管理员 |
| 关联知识库 | Document | 查看规范和知识 | 所有人 |
| 使用设备 | Monitor | 查看使用设备列表 | 所有人 |

### 7.4 对话框
- **详情对话框**：展示技能完整信息和使用说明
- **知识库对话框**：Tabs展示安全规范和处理知识
- **设备列表对话框**：表格展示使用该技能的设备

---

## 八、技术实现

### 8.1 数据来源
```typescript
// 基于常量初始化技能数据
import { AI_SKILLS } from '@/utils/copper-mine-constants';

const staticSkillData: AISkillVO[] = AI_SKILLS.map((skill, index) => ({
  skillId: skill.id,
  skillName: skill.name,
  description: skill.description,
  category: skill.category,
  icon: skill.icon,
  color: skill.color,
  status: index < 6 ? 'enabled' : 'disabled',
  workTypes: skill.workTypes,
  deviceUsageCount: index < 6 ? Math.floor(Math.random() * 15) + 5 : 0,
  detectionCount: index < 6 ? Math.floor(Math.random() * 500) + 100 : 0,
  violationCount: index < 6 ? Math.floor(Math.random() * 50) + 10 : 0,
  createTime: '2024-01-10 09:00:00',
  updateTime: formatDateTime()
}));
```

### 8.2 状态切换逻辑
```typescript
const handleToggleStatus = (row: AISkillVO) => {
  const action = row.status === 'enabled' ? '停用' : '启用';
  
  // 1. 确认操作
  proxy?.$modal.confirm(`确认${action}【${row.skillName}】技能吗？`)
    .then(() => {
      // 2. 更新状态
      const index = allSkillList.value.findIndex(item => item.skillId === row.skillId);
      if (index !== -1) {
        allSkillList.value[index].status = row.status === 'enabled' ? 'disabled' : 'enabled';
        allSkillList.value[index].updateTime = formatDateTime();
        
        // 3. 停用时清空统计数据
        if (allSkillList.value[index].status === 'disabled') {
          allSkillList.value[index].deviceUsageCount = 0;
          allSkillList.value[index].detectionCount = 0;
          allSkillList.value[index].violationCount = 0;
        }
      }
      
      proxy?.$modal.msgSuccess(`${action}成功`);
      getList();
    });
};
```

### 8.3 关联数据获取
```typescript
// 获取关联规范（模拟）
const handleViewKnowledge = (row: AISkillVO) => {
  // 根据技能的workTypes查询相关规范
  relatedRegulations.value = regulations.filter(r => 
    row.workTypes.includes(r.workType)
  );
  
  // 查询相关处理知识
  relatedHandling.value = handlingKnowledge.filter(h => 
    row.workTypes.includes(h.workType)
  );
};

// 获取使用设备（模拟）
const handleViewDevices = (row: AISkillVO) => {
  // 查询配置了该技能的设备
  relatedDevices.value = devices.filter(d => 
    d.configuredSkills.includes(row.skillId)
  );
};
```

---

## 九、扩展规划

### 9.1 短期扩展
- [ ] 技能配置历史记录（谁在什么时候启用/停用了技能）
- [ ] 技能效果评分系统（识别准确率、误报率）
- [ ] 技能使用趋势图表（按时间统计）
- [ ] 技能依赖关系管理（某些技能需要其他技能支持）

### 9.2 中期扩展
- [ ] 技能版本管理（技能算法升级）
- [ ] 技能A/B测试（对比不同版本效果）
- [ ] 技能推荐系统（根据作业类型推荐技能）
- [ ] 技能效果预警（准确率下降自动告警）

### 9.3 长期扩展
- [ ] 技能训练数据管理
- [ ] 自定义技能开发平台
- [ ] 技能市场（技能共享和交易）
- [ ] 联邦学习支持（多矿区协同训练）

---

## 十、总结

### 核心价值
1. ✅ **统一管理**：所有AI技能集中管理，避免分散
2. ✅ **动态控制**：灵活启停，不影响已运行设备
3. ✅ **数据透明**：实时统计，效果一目了然
4. ✅ **业务打通**：与设备、视频、规范、知识库全面关联

### 技术特点
1. ✅ **基于常量初始化**：复用`copper-mine-constants.ts`中的技能定义
2. ✅ **类型安全**：完整的TypeScript类型定义
3. ✅ **响应式统计**：使用computed实时计算统计数据
4. ✅ **模块化设计**：清晰的功能划分和数据流转

### 业务价值
1. ✅ **提升效率**：批量操作，快速管理
2. ✅ **保证质量**：统计数据支持效果评估
3. ✅ **灵活调整**：根据实际需求随时调整技能配置
4. ✅ **决策支持**：数据驱动的技能优化决策

---

**文档版本**：v1.0  
**创建时间**：2024-11-21  
**维护部门**：技术研发部  
**关联模块**：设备台账、技能配置、视频管理、规范库、知识库

