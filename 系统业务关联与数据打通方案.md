# 铜矿AI安全监控系统 - 业务关联与数据打通方案

## 一、方案概述

### 1.1 背景
为了实现铜矿AI安全监控系统各模块之间的真实业务关联，我们创建了**全局数据管理Store**和**综合数据看板**，实现了从设备部署、技能配置、视频识别到违规处理的完整数据流转。

### 1.2 核心组件

#### 📦 全局数据Store
**文件路径**：`/src/store/modules/copperMineData.ts`

**功能**：
- 统一管理所有业务数据
- 提供数据查询和操作接口
- 实现模块间数据共享
- 自动更新关联统计

#### 📊 综合数据看板
**文件路径**：`/src/views/dashboard/index.vue`

**功能**：
- 实时展示全矿数据
- 多维度统计分析
- 业务关联可视化
- 快速导航入口

---

## 二、数据模型与关联关系

### 2.1 核心数据模型

```typescript
// 设备信息
interface DeviceVO {
  deviceId: string;
  department: string;
  principal: string;
  status: 'online' | 'offline' | 'maintenance' | 'fault';
  configuredSkills: string[];    // 🔗 关联：已配置的技能ID列表
  videoCount: number;             // 🔗 关联：采集的视频数量
  violationCount: number;         // 🔗 关联：识别的违规数量
}

// AI技能信息
interface AISkillVO {
  skillId: string;
  status: 'enabled' | 'disabled';
  deviceUsageCount: number;       // 🔗 关联：使用该技能的设备数
  detectionCount: number;         // 🔗 关联：识别次数（视频数）
  violationCount: number;         // 🔗 关联：识别出的违规数
  hasPromptConfig: boolean;       // 🔗 关联：是否已配置提示词
}

// 视频信息
interface VideoVO {
  videoId: string;
  deviceId: string;               // 🔗 关联：所属设备
  detectedSkills: string[];       // 🔗 关联：使用的检测技能
  violationCount: number;         // 🔗 关联：违规数量
  violations: ViolationResult[];  // 🔗 关联：违规结果列表
}

// 违规结果
interface ViolationResult {
  videoId: string;                // 🔗 关联：所属视频
  skillId: string;                // 🔗 关联：检测技能
  regulationId: string;           // 🔗 关联：违反的规范
  knowledgeId: string;            // 🔗 关联：处理知识
  handled: boolean;               // 🔗 关联：处理状态
}

// 安全规范
interface RegulationVO {
  regulationId: string;
  workType: string;
  relatedSkills: string[];        // 🔗 关联：相关技能
  referenceCount: number;         // 🔗 关联：被引用次数
}

// 处理知识
interface HandlingKnowledgeVO {
  knowledgeId: string;
  violationName: string;
  relatedSkills: string[];        // 🔗 关联：相关技能
  usageCount: number;             // 🔗 关联：使用次数
  effectRating: number;           // 🔗 关联：效果评分
}
```

### 2.2 关联关系图

```
                        ┌─────────────────┐
                        │  提示词配置      │
                        │  (Prompt)       │
                        └────────┬────────┘
                                 │ hasPromptConfig
                                 ▼
┌──────────────┐        ┌─────────────────┐        ┌──────────────┐
│  设备台账    │◄───────┤  AI技能管理     ├───────►│  规范库      │
│  (Device)    │configuredSkills  (AISkill)│relatedSkills (Regulation)│
└──────┬───────┘        └────────┬────────┘        └──────┬───────┘
       │deviceId                 │skillId                 │regulationId
       │                         │                        │
       ▼                         ▼                        ▼
┌──────────────┐        ┌─────────────────┐        ┌──────────────┐
│  视频管理    │◄───────┤  违规结果       ├───────►│  知识库      │
│  (Video)     │detectedSkills│(Violation)  │knowledgeId  (Knowledge)  │
└──────────────┘        └─────────────────┘        └──────────────┘
```

---

## 三、业务流转场景

### 场景1：新设备部署到违规识别全流程

#### 第1步：设备入库（设备台账模块）
```typescript
// 1. 在设备台账中登记设备
const newDevice = {
  deviceId: 'DEV021',
  department: '采矿部',
  principal: '张三',
  status: 'online',
  configuredSkills: [],  // 初始为空
  videoCount: 0,
  violationCount: 0
};
store.devices.push(newDevice);
```

**数据状态**：
- ✅ 设备已入库
- ⏳ 未配置技能
- ⏳ 无视频数据
- ⏳ 无违规记录

---

#### 第2步：启用AI技能（AI技能管理模块）
```typescript
// 2. 启用需要的技能
const skill = store.getSkillById('violation_detection');
store.toggleSkillStatus('violation_detection'); // enabled

// 数据自动关联
skill.status = 'enabled';
skill.hasPromptConfig = true;  // 已配置提示词
```

**数据状态**：
- ✅ 技能已启用
- ✅ 提示词已配置
- ⏳ 未绑定到设备
- ⏳ 使用数为0

---

#### 第3步：配置提示词（提示词配置模块）
```typescript
// 3. 为技能配置专业提示词
const prompt = {
  skillId: 'violation_detection',
  systemPrompt: '你是一个专业的铜矿安全监控AI助手...',
  userPrompt: '请分析视频中的违规行为...'
};
store.promptConfigs.push(prompt);

// 数据自动关联
skill.hasPromptConfig = true;
```

**数据状态**：
- ✅ 提示词已配置
- ✅ 技能准备就绪
- ⏳ 等待设备绑定

---

#### 第4步：设备绑定技能（技能配置模块）
```typescript
// 4. 为设备配置技能
store.configureDeviceSkills('DEV021', [
  'violation_detection',
  'ppe_detection',
  'welding_standard'
]);

// 数据自动关联更新
device.configuredSkills = ['violation_detection', 'ppe_detection', 'welding_standard'];
skill.deviceUsageCount++;  // 使用设备数+1
```

**数据状态**：
- ✅ 设备已配置技能
- ✅ 技能使用数更新
- ⏳ 等待视频采集

---

#### 第5步：视频采集与AI识别（视频管理模块）
```typescript
// 5. 设备采集视频并进行AI识别
const video = {
  videoId: 'VID_DEV021_001',
  deviceId: 'DEV021',
  detectedSkills: device.configuredSkills,  // 使用设备配置的技能
  aiStatus: 'processing'
};
store.addVideo(video);

// AI识别过程（使用配置的提示词）
const prompt = store.getPromptBySkillId('violation_detection');
const result = await callAIModel({
  systemPrompt: prompt.systemPrompt,
  userPrompt: prompt.userPrompt,
  videoData: video
});

// 数据自动关联更新
device.videoCount++;              // 设备视频数+1
skill.detectionCount++;           // 技能识别次数+1
```

**数据状态**：
- ✅ 视频已采集
- ✅ AI识别进行中
- ⏳ 等待识别结果

---

#### 第6步：识别到违规（违规结果）
```typescript
// 6. AI识别到违规行为
const violation = {
  resultId: 'VR_001',
  videoId: 'VID_DEV021_001',
  skillId: 'violation_detection',
  violationType: '未佩戴安全帽',
  severity: 'high',
  confidence: 0.92,
  regulationId: 'REG001',  // 关联规范
  knowledgeId: 'KNW001',   // 关联处理知识
  handled: false
};
store.addViolationResult(violation);

// 数据自动关联更新
video.violationCount++;           // 视频违规数+1
video.violations.push(violation);
device.violationCount++;          // 设备违规数+1
skill.violationCount++;           // 技能违规识别数+1
regulation.referenceCount++;      // 规范引用次数+1
```

**数据状态**：
- ✅ 识别到违规
- ✅ 关联规范和知识库
- ⏳ 等待处理

---

#### 第7步：查询处理知识（违规处理知识库）
```typescript
// 7. 查询处理知识
const knowledge = store.getKnowledgeByViolationType('未佩戴安全帽');

// 显示处理建议
console.log(knowledge.immediateAction);  // 立即处理措施
console.log(knowledge.penalty);          // 处罚措施
console.log(knowledge.rectification);    // 整改要求
console.log(knowledge.prevention);       // 预防措施
```

**数据状态**：
- ✅ 获取处理建议
- ✅ 知识库提供支撑
- ⏳ 等待执行处理

---

#### 第8步：处理违规（违规处理）
```typescript
// 8. 安全管理员处理违规
store.handleViolation('VR_001', '李经理');

// 数据自动关联更新
violation.handled = true;
violation.handleTime = formatDateTime();
violation.handler = '李经理';
knowledge.usageCount++;  // 知识库使用次数+1
```

**数据状态**：
- ✅ 违规已处理
- ✅ 处理记录完整
- ✅ 知识库使用统计更新

---

### 完整流程数据流转图

```
设备入库 ──► AI技能启用 ──► 提示词配置 ──► 设备绑定技能
   │             │              │              │
   │             │              │              │
   ▼             ▼              ▼              ▼
 Device      AISkill        Prompt      Device.configuredSkills
   │             │              │              │
   └─────────────┴──────────────┴──────────────┘
                        │
                        ▼
                  视频采集与AI识别
               （使用配置的提示词）
                        │
                        ▼
                   识别到违规
                        │
         ┌──────────────┼──────────────┐
         │              │              │
         ▼              ▼              ▼
    关联规范        关联知识库      记录违规
    Regulation      Knowledge      Violation
         │              │              │
         └──────────────┼──────────────┘
                        │
                        ▼
                   执行处理
                        │
                        ▼
              更新统计数据
         （所有关联数据自动更新）
```

---

## 四、数据查询接口（Store Getters）

### 4.1 设备相关查询

```typescript
// 获取在线设备列表
const onlineDevices = store.onlineDevices;

// 获取设备总数
const deviceCount = store.deviceCount;

// 根据ID获取设备
const device = store.getDeviceById('DEV001');

// 获取设备配置的技能列表
const skills = store.getDeviceSkills('DEV001');
```

### 4.2 技能相关查询

```typescript
// 获取已启用的技能
const enabledSkills = store.enabledSkills;

// 根据ID获取技能
const skill = store.getSkillById('violation_detection');

// 获取使用某技能的设备列表
const devices = store.getSkillDevices('violation_detection');
```

### 4.3 视频和违规查询

```typescript
// 获取设备的视频列表
const videos = store.getDeviceVideos('DEV001');

// 获取视频的违规结果
const violations = store.getVideoViolations('VID_001');

// 获取待处理的违规
const pendingViolations = store.pendingViolations;
```

### 4.4 规范和知识库查询

```typescript
// 根据作业类型获取规范
const regulations = store.getRegulationsByWorkType('焊割作业');

// 根据技能ID获取关联规范
const skillRegulations = store.getRegulationsBySkill('welding_standard');

// 根据违规类型获取处理知识
const knowledge = store.getKnowledgeByViolationType('未佩戴安全帽');

// 根据技能ID获取关联知识
const skillKnowledge = store.getKnowledgeBySkill('ppe_detection');
```

### 4.5 统计分析查询

```typescript
// 获取总体统计
const stats = store.overallStats;
/*
{
  deviceCount: 20,
  onlineDeviceCount: 17,
  enabledSkillCount: 6,
  totalVideoCount: 150,
  totalViolationCount: 89,
  pendingViolationCount: 12,
  regulationCount: 7,
  knowledgeCount: 5
}
*/

// 按部门统计违规
const deptStats = store.violationsByDepartment;
/*
{
  '采矿部': 35,
  '安全部': 18,
  '技术部': 12,
  ...
}
*/

// 按技能统计识别效果
const skillStats = store.detectionStatsBySkill;
/*
{
  'violation_detection': {
    detectionCount: 50,
    violationCount: 30,
    accuracy: 0.85
  },
  ...
}
*/
```

---

## 五、在各模块中使用Store

### 5.1 设备台账模块集成

```vue
<script setup>
import { useCopperMineStore } from '@/store/modules/copperMineData';

const store = useCopperMineStore();

// 显示设备配置的技能
const getDeviceSkillNames = (deviceId: string) => {
  const skills = store.getDeviceSkills(deviceId);
  return skills.map(s => s.skillName).join('、');
};

// 显示设备的违规统计
const getDeviceViolationCount = (deviceId: string) => {
  const device = store.getDeviceById(deviceId);
  return device?.violationCount || 0;
};
</script>
```

### 5.2 技能配置模块集成

```vue
<script setup>
import { useCopperMineStore } from '@/store/modules/copperMineData';

const store = useCopperMineStore();

// 只显示已启用的技能
const availableSkills = computed(() => store.enabledSkills);

// 配置设备技能时更新Store
const handleConfigureSkills = (deviceId: string, skillIds: string[]) => {
  store.configureDeviceSkills(deviceId, skillIds);
  ElMessage.success('配置成功');
};

// 显示技能使用统计
const getSkillUsageInfo = (skillId: string) => {
  const skill = store.getSkillById(skillId);
  return {
    deviceCount: skill?.deviceUsageCount || 0,
    detectionCount: skill?.detectionCount || 0,
    violationCount: skill?.violationCount || 0
  };
};
</script>
```

### 5.3 视频管理模块集成

```vue
<script setup>
import { useCopperMineStore } from '@/store/modules/copperMineData';

const store = useCopperMineStore();

// 显示视频使用的技能
const getVideoSkills = (video: VideoVO) => {
  return video.detectedSkills
    .map(skillId => store.getSkillById(skillId))
    .filter(Boolean)
    .map(skill => skill!.skillName);
};

// 显示违规的规范和知识
const getViolationDetails = (violation: ViolationResult) => {
  const regulation = store.regulations.find(r => r.regulationId === violation.regulationId);
  const knowledge = store.handlingKnowledge.find(k => k.knowledgeId === violation.knowledgeId);
  
  return {
    regulation: regulation?.standards,
    handling: knowledge?.immediateAction
  };
};

// 处理违规时更新Store
const handleProcessViolation = (resultId: string, handler: string) => {
  store.handleViolation(resultId, handler);
  ElMessage.success('处理成功');
};
</script>
```

### 5.4 规范库模块集成

```vue
<script setup>
import { useCopperMineStore } from '@/store/modules/copperMineData';

const store = useCopperMineStore();

// 显示规范被引用次数
const getRegulationUsage = (regulationId: string) => {
  const regulation = store.regulations.find(r => r.regulationId === regulationId);
  return regulation?.referenceCount || 0;
};

// 显示关联的技能
const getRelatedSkills = (regulationId: string) => {
  const regulation = store.regulations.find(r => r.regulationId === regulationId);
  if (!regulation) return [];
  
  return regulation.relatedSkills
    .map(skillId => store.getSkillById(skillId))
    .filter(Boolean);
};
</script>
```

### 5.5 知识库模块集成

```vue
<script setup>
import { useCopperMineStore } from '@/store/modules/copperMineData';

const store = useCopperMineStore();

// 显示知识库使用次数
const getKnowledgeUsage = (knowledgeId: string) => {
  const knowledge = store.handlingKnowledge.find(k => k.knowledgeId === knowledgeId);
  return knowledge?.usageCount || 0;
};

// 显示效果评分
const getKnowledgeRating = (knowledgeId: string) => {
  const knowledge = store.handlingKnowledge.find(k => k.knowledgeId === knowledgeId);
  return knowledge?.effectRating || 0;
};

// 显示关联的违规案例
const getRelatedViolations = (knowledgeId: string) => {
  return store.violations.filter(v => v.knowledgeId === knowledgeId);
};
</script>
```

---

## 六、综合数据看板功能

### 6.1 核心指标展示

```typescript
// 实时统计4大核心指标
{
  设备总数: 20台,
  在线设备: 17台,
  采集视频: 150个,
  今日视频: 12个,
  识别违规: 89次,
  待处理: 12次,
  启用技能: 6个,
  技能总数: 8个
}
```

### 6.2 设备技能配置情况

展示每个技能的使用情况：
- 使用设备数
- 识别次数
- 违规数量

### 6.3 部门违规统计

按部门统计：
- 设备数量
- 视频数量
- 违规数量
- 违规率

### 6.4 违规类型TOP10

统计最常见的违规类型：
- 发生次数
- 平均置信度

### 6.5 违规处理情况

展示处理统计：
- 已处理/待处理数量
- 处理率
- 处理人排行
- 平均处理时长

### 6.6 最新违规识别

实时展示最新的违规：
- 识别时间
- 设备信息
- 违规类型
- 严重程度
- 处理状态

---

## 七、实际应用场景

### 场景1：安全经理查看全矿安全态势

```
1. 打开【综合数据看板】
2. 查看核心指标：
   - 设备在线率：85%（17/20）
   - 今日违规：12次
   - 待处理违规：8次
3. 查看部门违规统计：
   - 采矿部违规率最高（15%）
   - 安全部违规率最低（5%）
4. 点击【查看详情】跳转到具体模块处理
```

### 场景2：技术人员优化AI识别效果

```
1. 打开【综合数据看板】
2. 查看设备技能配置情况：
   - 违规行为识别：使用12台设备，识别50次，违规30次
   - 置信度统计：平均85%
3. 发现某技能识别率不高
4. 点击跳转到【提示词配置】
5. 优化提示词配置
6. 创建新版本测试
7. 回到看板查看效果提升
```

### 场景3：安全管理员处理违规

```
1. 打开【综合数据看板】
2. 查看【最新违规识别】：
   - 发现新违规：未佩戴安全帽
   - 设备：DEV001
   - 部门：采矿部
   - 责任人：张三
3. 点击【处理】
4. 系统自动显示：
   - 违反的规范：REG001
   - 处理建议（来自知识库）
5. 执行处理：
   - 拍照记录
   - 罚款100元
   - 安全培训
6. 标记为已处理
7. 统计自动更新
```

### 场景4：部门负责人查看本部门情况

```
1. 打开【综合数据看板】
2. 查看【部门违规统计】找到采矿部
3. 看到数据：
   - 设备数：8台
   - 视频数：75个
   - 违规数：35次
   - 违规率：46.7%（偏高）
4. 点击【查看详情】
5. 查看具体违规类型和责任人
6. 制定整改措施：
   - 加强安全教育
   - 增加巡检频次
   - 责任人谈话
```

---

## 八、数据自动更新机制

### 8.1 统计数据自动更新

```typescript
// Store中的updateStatistics方法
updateStatistics() {
  // 1. 更新设备统计
  this.devices.forEach(device => {
    device.videoCount = this.videos.filter(v => v.deviceId === device.deviceId).length;
    device.violationCount = this.violations.filter(v => {
      const video = this.videos.find(vid => vid.videoId === v.videoId);
      return video && video.deviceId === device.deviceId;
    }).length;
  });
  
  // 2. 更新技能统计
  this.aiSkills.forEach(skill => {
    skill.deviceUsageCount = this.devices.filter(d => d.configuredSkills.includes(skill.skillId)).length;
    skill.detectionCount = this.videos.filter(v => v.detectedSkills.includes(skill.skillId)).length;
    skill.violationCount = this.violations.filter(v => v.skillId === skill.skillId).length;
  });
  
  // 3. 更新规范引用次数
  this.regulations.forEach(reg => {
    reg.referenceCount = this.violations.filter(v => v.regulationId === reg.regulationId).length;
  });
  
  // 4. 更新知识库使用次数
  this.handlingKnowledge.forEach(k => {
    k.usageCount = this.violations.filter(v => v.knowledgeId === k.knowledgeId).length;
  });
}
```

### 8.2 触发更新的时机

- ✅ 添加新设备 → 更新设备统计
- ✅ 配置设备技能 → 更新技能使用数
- ✅ 添加新视频 → 更新设备视频数、技能识别次数
- ✅ 添加违规结果 → 更新所有相关统计
- ✅ 处理违规 → 更新知识库使用次数
- ✅ 切换技能状态 → 更新技能统计

---

## 九、优势总结

### 9.1 数据一致性
- ✅ 单一数据源（Store）
- ✅ 自动更新统计
- ✅ 避免数据不一致

### 9.2 业务关联性
- ✅ 模块间数据打通
- ✅ 完整业务流转
- ✅ 关联关系清晰

### 9.3 开发效率
- ✅ 统一的数据接口
- ✅ 丰富的查询方法
- ✅ 减少重复代码

### 9.4 用户体验
- ✅ 数据实时更新
- ✅ 综合看板直观
- ✅ 快速导航便捷

### 9.5 扩展性
- ✅ 易于添加新模块
- ✅ 易于添加新统计
- ✅ 易于对接后端API

---

## 十、后续扩展方向

### 10.1 数据持久化
```typescript
// 使用IndexedDB或LocalStorage
watch(store.devices, (newDevices) => {
  localStorage.setItem('copperMine_devices', JSON.stringify(newDevices));
}, { deep: true });
```

### 10.2 实时数据同步
```typescript
// WebSocket实时推送
const ws = new WebSocket('ws://server.com/coppermine');
ws.onmessage = (event) => {
  const { type, data } = JSON.parse(event.data);
  if (type === 'NEW_VIOLATION') {
    store.addViolationResult(data);
  }
};
```

### 10.3 导出完整报告
```typescript
// 导出PDF报告
const exportReport = () => {
  const reportData = {
    stats: store.overallStats,
    violations: store.violations,
    devices: store.devices,
    // ... 更多数据
  };
  generatePDFReport(reportData);
};
```

### 10.4 权限控制
```typescript
// 根据角色过滤数据
const visibleDevices = computed(() => {
  if (currentUser.role === 'department_manager') {
    return store.devices.filter(d => d.department === currentUser.department);
  }
  return store.devices;
});
```

---

## 十一、总结

通过创建**全局数据Store**和**综合数据看板**，我们实现了：

1. ✅ **数据统一管理**：所有业务数据集中管理
2. ✅ **模块完全打通**：9大模块数据实时关联
3. ✅ **业务流转顺畅**：从设备到处理的完整闭环
4. ✅ **统计自动更新**：关联数据自动计算
5. ✅ **可视化直观**：综合看板一目了然

现在系统不仅功能完整，而且各模块之间形成了**真实的业务关联**，完全符合铜矿实际工作需求！

---

**文档版本**：v1.0  
**创建时间**：2024-11-21  
**维护部门**：技术研发部  
**相关文件**：
- `/src/store/modules/copperMineData.ts` - 全局数据Store
- `/src/views/dashboard/index.vue` - 综合数据看板
- 各业务模块集成代码

